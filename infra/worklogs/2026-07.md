# 2026-07 Infra Worklog

## 2026-07-01 14:36 - Frontend Intelligence 프록시 내부 URL 추가

완료: AWS dev `frontend` 컨테이너 환경변수에 `INTELLIGENCE_API_BASE_URL=http://intelligence-service:8086`를 추가했다. 운영 프론트의 `/api/intelligence/*` Next route handler가 컨테이너 내부 기본값 `localhost:8086`으로 붙어 500을 내던 문제를, Compose service DNS 기준으로 Intelligence-Service에 프록시하도록 수정했다.

검증: 운영 필수 환경변수를 더미 값으로 채운 뒤 `docker compose -f infra/aws-dev/deploy/docker-compose.yml config --quiet` 통과.

## 2026-07-01 17:52 - Intelligence 프론트 API 경로 `/api/v1` 전환

완료: `brainx-next` Intelligence client의 `/api/intelligence/*` 호출을 SSOT 공개 계약인 `/api/v1/*`로 전환했다. 로컬 개발에서는 `next.config.mjs`의 Intelligence 전용 rewrite가 Gateway rewrite보다 먼저 적용되도록 했고, AWS dev Caddy direct route에 `/api/v1/users/me/style-profile`을 추가했다.

검증: `npm run typecheck` 통과. `git diff --check -- brainx-next/lib/intelligence-api.ts brainx-next/next.config.mjs infra/aws-dev/deploy/Caddyfile infra/aws-dev/README.md infra/worklogs/2026-07.md` 통과(LF/CRLF 경고만 출력). `docker compose -f infra/aws-dev/deploy/docker-compose.yml config --quiet` 통과(로컬 미설정 env 경고만 출력). `next.config.mjs` rewrites 출력으로 Intelligence 전용 route가 broad Gateway rewrite보다 먼저 적용됨을 확인했다. 로컬 Docker daemon 미실행으로 `caddy validate`는 실행하지 못했다.

## 2026-07-02 14:42 - Grafana/Prometheus monitoring scaffold added

완료: BrainX AWS dev compose에 `prometheus` 컨테이너와 `grafana` datasource provisioning을 추가하고, `User-Service`, `Gateway-Service`, `Workspace-Service`에 `micrometer-registry-prometheus` 의존성과 `/actuator/prometheus` 노출을 붙였다. Grafana는 Caddy 뒤의 `/grafana/` 경로로만 열고, Prometheus는 내부 Docker 네트워크에서만 서비스 메트릭을 스크랩하도록 구성했다.

검증: `docker compose -f infra/aws-dev/deploy/docker-compose.yml config --quiet`, `git diff --check`.

## 2026-07-02 15:18 - Mcp-Service 인증/배포 v1 추가

완료: `brainX_back/Mcp-Service`를 Spring Boot 3.5 / Java 21 / Spring AI MCP stateless HTTP 서비스로 추가했다. 사용자 JWT 기반 API key 발급/목록/폐기 API, API key 기반 `whoami`, MCP `/mcp` 보호, `brainx_whoami` tool을 구현했다. 로컬 Gateway/Compose, AWS dev Caddy/Compose/remote deploy, ECR Terraform repository map, 변경 감지 스크립트, PR/deploy workflow, README/runtime 문서에 `mcp-service`를 반영했다.

검증: `Mcp-Service` `.\gradlew.bat --no-daemon check --console=plain` 통과. `Gateway-Service` `.\gradlew.bat --no-daemon check --console=plain` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). `docker compose -f brainX_back/docker-compose.yml --profile apps config --no-env-resolution --quiet` 통과. AWS dev compose는 더미 env로 `docker compose --env-file <temp> -f infra/aws-dev/deploy/docker-compose.yml config --quiet` 통과. `C:\Program Files\Git\bin\bash.exe -n infra/aws-dev/scripts/deploy_remote.sh` 통과. `terraform -chdir=infra/aws-dev/terraform fmt -check -recursive` 및 `terraform -chdir=infra/aws-dev/terraform validate` 통과. `npx --yes @redocly/cli lint contracts-v2/brainx-openapi.ssot.yaml` 경고 없이 통과. AsyncAPI SSOT는 변경하지 않았고, 로컬 `npx @asyncapi/cli`는 cache dependency 누락(`is-docker`)으로 실패, Docker validator는 Docker daemon 미실행으로 실행하지 못했다.

## 2026-07-02 15:46 - AWS dev 배포 workflow concurrency 추가

완료: `.github/workflows/brainx-dev-deploy.yml`에 workflow-level `concurrency`를 추가해 `brainx-dev-deploy` group 기준으로 dev 배포 run이 동시에 실행되지 않게 했다. `cancel-in-progress: false`로 실행 중인 배포는 중단하지 않고 다음 run이 pending 상태로 대기하도록 설정했다.

검증: `git diff --check -- .github/workflows/brainx-dev-deploy.yml` 통과(LF/CRLF 경고만 출력).

## 2026-07-02 16:12 - AWS dev Docker Buildx ECR 캐시 도입

완료: `brainx-dev-deploy.yml`의 배포 이미지 빌드를 Docker Buildx 기반 `buildx build --push`로 전환하고, 각 서비스 ECR repository의 `buildcache` tag를 registry cache로 사용하도록 했다. Spring Boot 배포 Dockerfile에는 Gradle cache mount를, Next.js 배포 Dockerfile에는 npm cache mount를 추가했다. 배포 context별 최소 `.dockerignore`를 추가해 build context가 불필요한 build/cache 산출물을 포함하지 않도록 정리했다.

검증: `docker buildx version` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). 더미 env 기반 `docker compose --env-file <temp> -f infra/aws-dev/deploy/docker-compose.yml config --quiet` 통과. PyYAML로 `brainx-dev-deploy.yml` 파싱 및 concurrency/job 키 확인 통과.

## 2026-07-02 16:13 - AWS dev 배포 겹침 트러블슈팅 문서화

완료: `infra/aws-dev/troubleshooting.md`를 추가해 PR #114, PR #113의 main deploy run 겹침으로 public endpoint verification이 실패한 사례를 표준 runbook으로 정리했다. 증상, GitHub Actions 확인, SSM 기반 EC2 진단, 최신 main run만 rerun하는 복구 절차, workflow-level `concurrency: group: brainx-dev-deploy`, `cancel-in-progress: false` 재발방지 기준을 문서화했다. `infra/INDEX.md`와 `infra/aws-dev/README.md`에서 새 troubleshooting 문서로 라우팅한다.

검증: 문서 전용 변경이다. `git diff --check -- infra/aws-dev/troubleshooting.md infra/aws-dev/README.md infra/INDEX.md infra/worklogs/2026-07.md .github/workflows/brainx-dev-deploy.yml`로 whitespace와 conflict marker가 없는지 확인했다.

## 2026-07-02 16:45 - AWS dev 반복 배포 속도 개선

완료: `brainx-dev-deploy.yml`의 Buildx cache backend를 서비스별 GitHub Actions cache scope로 변경했다. remote deploy는 SSM Parameter Store 값을 batch로 읽고, 현재 RDS target 기준 DB bootstrap marker가 있으면 DB 생성 확인을 건너뛰도록 했다. config-only deploy에서는 전체 image pull을 하지 않고 compose 재적용만 수행하며, GitHub endpoint verification은 변경된 service category 기준으로 필요한 endpoint만 확인하도록 줄였다.

검증: `C:\Program Files\Git\bin\bash.exe -n infra/aws-dev/scripts/deploy_remote.sh` 통과. `docker compose -f infra/aws-dev/deploy/docker-compose.yml config --quiet` 통과(로컬 미설정 env 경고만 출력). `python infra/aws-dev/scripts/detect_changed_services.py --base origin/main --head HEAD` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). 실제 AWS deploy는 별도 workflow 실행이 필요하다.

## 2026-07-02 16:28 - PR #115 Codex review 반영

완료: MCP API key와 사용자 JWT가 함께 들어오는 요청에서 API key 인증이 유지되도록 `JwtAuthenticationFilter`를 보강했다. 기존 local Postgres volume에서도 `brainx_mcp` DB가 생성되도록 compose one-shot DB bootstrap을 추가했고, Admin-Service health 수집에 Mcp-Service를 포함했다. Graph 화면은 노트 refresh 후 AI cluster latest 상태를 다시 읽도록 graph data version 의존성을 추가했다.

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.mcp.security.McpSecurityMvcTest --console=plain` 통과. `.\gradlew.bat --no-daemon test --tests com.brainx.admin.service.AdminServiceMonitoringSnapshotTest --console=plain` 통과. `npm run typecheck` 통과. `docker compose -f brainX_back/docker-compose.yml --profile apps config --no-env-resolution --quiet` 통과. `docker compose -f infra/aws-dev/deploy/docker-compose.yml config --quiet` 통과(로컬 미설정 env 경고만 출력). `C:\Program Files\Git\bin\bash.exe -n brainX_back/docker/postgres/init/01-create-service-databases.sh` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-02 15:39 - Grafana dashboard application tag fix

완료: `Spring Boot 2.1 System Monitor` 대시보드가 각 서비스의 `application` 메트릭 태그를 전제로 해서, `User-Service`, `Gateway-Service`, `Workspace-Service`에 `management.metrics.tags.application=${spring.application.name}`를 추가했다. 이제 Dashboard의 `Application`/`Instance`/`HikariCP-Pool` 변수와 패널이 서비스별 시계열을 제대로 찾을 수 있어야 한다.

검증: `git diff --check -- infra/aws-dev/README.md infra/worklogs/2026-07.md brainX_back/User-Service/src/main/resources/application.yml brainX_back/Gateway-Service/src/main/resources/application.yml brainX_back/Workspace-Service/src/main/resources/application.yml`.

## 2026-07-02 15:55 - Admin-Service direct service DNS fix

Fix: Added Docker DNS URLs for admin-service direct calls so login/dashboard requests do not fall back to localhost.

Validation: docker compose -f infra/aws-dev/deploy/docker-compose.yml config --quiet

## 2026-07-02 16:05 - Gateway Prometheus endpoint allowlist

Fix: Added `/actuator/prometheus` to Gateway-Service public paths so Prometheus can scrape the gateway target without hitting the global auth filter.

Validation: Gateway auth filter test updated to allow `/actuator/prometheus`.

## 2026-07-02 16:40 - Micrometer JVM extras aligned for dashboard panels

Fix: Added `io.github.mweirauch:micrometer-jvm-extras:0.2.2` to `User-Service`, `Gateway-Service`, and `Workspace-Service` so the imported Grafana JVM dashboard can resolve the extra JVM and process meters instead of showing `N/A`.

Validation: build files updated; follow-up build/deploy required to refresh the service images.

## 2026-07-02 17:10 - Grafana dashboard provisioning now bypasses DS_PROMETHEUS placeholder

Fix: Added Grafana dashboard file provisioning and a provisioned `spring_boot_21` dashboard so the AWS dev stack no longer depends on the UI import placeholder `${DS_PROMETHEUS}`. The dashboard now references the provisioned `Prometheus` datasource directly and renders fixed per-service panels for User-Service, Gateway-Service, and Workspace-Service.

Validation: Grafana compose mount updated; dashboard JSON added under `infra/aws-dev/deploy/grafana/dashboards/`.

## 2026-07-02 17:25 - Open files panel query aligned with Micrometer metric name

Fix: Changed the dashboard's rightmost file-descriptor panels from `process_open_fds` to `process_files_open_files` so Grafana queries the Micrometer-style Prometheus metric name instead of a stale placeholder.

Validation: dashboard JSON re-parsed successfully after the query update.

## 2026-07-02 18:10 - Monitoring expanded to the remaining backend services

Fix: Added `admin-service`, `commerce-service`, `ingestion-service`, and `intelligence-service` to the Prometheus scrape config and provisioned matching Grafana stat panels so all seven backend services appear in one dashboard view.

Validation: backend actuator exposure, Prometheus jobs, and dashboard JSON were updated together in the same change set.

## 2026-07-02 18:32 - Grafana label fallback for stale images

Fix: Added scrape-time `application` labels to the Prometheus targets and made the dashboard's Open Files panels fall back to `process_open_fds` so the monitor still renders when a service image is one deploy behind the latest Micrometer changes.

Validation: Prometheus target labels and dashboard query expressions were updated together.

## 2026-07-02 18:48 - Dashboard switched to time series charts

Fix: Converted the monitoring dashboard panels from `Stat` tiles to `Time series` so Grafana shows the full line graphs in the dashboard, not just the mini sparkline preview.

Validation: dashboard JSON parses successfully after the panel type conversion.

## 2026-07-02 22:51 - Force recreate monitoring stack on config refresh

Fix: Updated the remote deploy path to force recreate containers when the AWS dev config bundle changes, so Prometheus and Grafana reload the bind-mounted scrape and dashboard files instead of keeping stale runtime state.

Validation: deploy script now uses `--force-recreate` in the config-refresh branch.

## 2026-07-02 22:59 - Document Grafana provisioning save flow

Fix: Documented that the Spring Boot monitor dashboard is file-provisioned from `/etc/grafana/dashboards/spring-boot-2-1-system-monitor.json`, that `Save JSON to file` is needed for persistent UI edits, and that Grafana should auto-reload from the provider interval or be restarted if needed.

Validation: README now matches the actual Grafana provisioning path and reload behavior.

## 2026-07-02 23:16 - Enable actuator on Ingestion-Service

Fix: Added `spring-boot-starter-actuator` to Ingestion-Service so `/actuator/prometheus` exists at runtime, matching the other monitored services and allowing Prometheus to scrape the JVM/process meters.

Validation: build dependency list now includes the actuator starter alongside `micrometer-registry-prometheus`.
