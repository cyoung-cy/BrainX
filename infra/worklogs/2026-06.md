# Infra Worklog - 2026-06
## 2026-06-30 22:47 - Admin-Service AWS runtime env 자동 주입

완료: AWS dev 배포 경로에서 Admin-Service가 쓰는 `ADMIN_DB_NAME`, `GATEWAY_SERVICE_URL`, `MAIL_HOST`, `MAIL_PORT`를 `deploy_remote.sh`가 `runtime.env`에 쓰고, `deploy/docker-compose.yml`이 그 값을 받도록 맞췄다. 기존 `SEED_ADMIN_LOGIN_ID`, `SEED_ADMIN_PASSWORD`, `SEED_ADMIN_NAME`는 이미 runtime 경로에 있었지만, 이번 수정으로 Admin-Service의 로컬 env와 AWS CI/CD env 구성이 더 잘 맞게 정리됐다.

검증: `git diff --check`와 `docker compose -f infra/aws-dev/deploy/docker-compose.yml config --quiet`로 문법 확인을 진행할 예정이다. AWS 실제 반영은 이 세션에 AWS CLI/자격증명이 없어 수행하지 않았다.

## 2026-06-29 17:44 - infra agent 문서 라우팅 추가

완료: `infra/AGENTS.md`와 `infra/INDEX.md`를 추가해 infra 하위 작업의 읽기 순서, 문서 경계, 검증/완료 루프, task routing을 정리했다. `agent-documentation-compounder` 기준에 맞춰 root 성격의 문서는 짧게 유지하고, AWS dev 배포 세부사항은 기존 `infra/aws-dev/README.md`로 라우팅했다.

검증: `rg`로 infra 관련 문서 구조와 기존 루트 `AGENTS.md`, SSOT YAML, README를 확인했다. 공개 API/AsyncAPI 계약 변경은 없음.

## 2026-06-29 18:10 - Ingestion-Service 배포 환경변수 보강

완료: AWS dev 원격 배포 스크립트가 SSM에서 `NOTION_CLIENT_ID`, `NOTION_CLIENT_SECRET`, `NOTION_REDIRECT_URI`, `CDN_BASE_URL`, `ASSET_STORAGE_DIR`를 읽어 runtime env에 쓰도록 추가했다. Docker Compose의 `ingestion-service`에 같은 환경변수를 전달하고, 현재 Ingestion-Service가 S3 adapter를 쓰지 않는 점을 고려해 `/app/asset-storage`를 `ingestion_asset_storage` named volume으로 영속화했다.

문서: `infra/aws-dev/README.md`에 Notion SSM parameter, `https://brainx.p-e.kr/notion-callback` redirect 등록, 노출된 Notion secret 재발급 필요, S3 asset bucket 미사용 제한을 추가했다.

AWS: `/brainx/dev/NOTION_CLIENT_ID`, `/brainx/dev/NOTION_REDIRECT_URI`, `/brainx/dev/CDN_BASE_URL`, `/brainx/dev/ASSET_STORAGE_DIR`를 SSM SecureString으로 추가했다. 채팅에 노출된 Notion client secret은 재사용하지 않았으므로 `/brainx/dev/NOTION_CLIENT_SECRET`은 재발급 값으로 별도 등록이 필요하다.

검증: `docker compose -f infra/aws-dev/deploy/docker-compose.yml config --quiet`, Git Bash `bash -n infra/aws-dev/scripts/deploy_remote.sh`, `git diff --check` 통과. 공개 API/AsyncAPI 계약 변경은 없음.

## 2026-06-30 09:48 - AWS dev EC2/RDS running 적용

완료: `infra/aws-dev/terraform`에서 `ec2_runtime_state=running`, `rds_runtime_state=running`으로 Terraform apply를 실행했다. EC2는 `aws_ec2_instance_state.app`가 `stopped -> running`으로 변경되었고, RDS는 Terraform output과 실제 AWS 상태가 어긋난 drift가 있어 `rds_runtime_state_operation_nonce`를 일시적으로 올려 RDS start helper를 재실행했다. 이후 nonce를 기본값 `0`으로 정규화했다.

검증: AWS CLI 기준 EC2 `i-0b5a622e94da9b41c`는 `running`, RDS `brainx-dev-postgres`는 `available`. 최종 `terraform plan -var="ec2_runtime_state=running" -var="rds_runtime_state=running" -var="rds_runtime_state_operation_nonce=0"` 결과 `No changes`.

## 2026-06-29 23:31 - AWS dev EC2/RDS runtime stop

완료: 비용 제어를 위해 AWS dev EC2 `i-0b5a622e94da9b41c`(`brainx-dev-app`)와 RDS `brainx-dev-postgres`에 stop 요청을 실행했다. Terraform `ec2_runtime_state`/`rds_runtime_state` plan을 먼저 확인했으나, 현재 설정에서 `aws_instance.app` `user_data` drift로 EC2 교체가 함께 계획되어 적용하지 않았다. 실제 중지는 AWS CLI `stop-instances`, `stop-db-instance`로 수행했다.

검증: `aws ec2 describe-instances --region ap-northeast-2 --instance-ids i-0b5a622e94da9b41c` 결과 `stopped`, `aws rds describe-db-instances --region ap-northeast-2 --db-instance-identifier brainx-dev-postgres` 결과 `stopped` 확인. SSOT/API 계약 변경 없음.

## 2026-06-30 10:36 - AWS dev Intelligence 재배포

완료: GitHub Actions `brainx-dev-deploy.yml` 최신 실패 run `28397384411`의 failed deploy job을 재실행해 `intelligence-service`, `frontend`를 SHA `ce0163bfa5aff5a9f8c52ae331c7dabd97693bda` 이미지로 재배포했다. 기존 실패 원인은 EC2가 SSM `SendCommand` 대상 valid state가 아니어서 발생한 `InvalidInstanceId`였다.

검증: run URL `https://github.com/Final-BrainX/BrainX/actions/runs/28397384411` 결과 `success`. EC2 `i-0b5a622e94da9b41c`는 `running`, SSM `Online`, command `2bf08778-b052-4f35-8542-685d77ed6a2c`는 `Success`. `brainx-intelligence-service`와 `brainx-frontend`가 새 SHA 이미지로 재생성되었고, `https://brainx.p-e.kr/` 및 `https://admin.brainx.p-e.kr/`는 `200`, `https://brainx.p-e.kr/api/v1/intelligence/ai/models`는 인증 경로 응답 `401` 확인.

## 2026-06-30 17:51 - AWS dev 환경변수 운영 문서화

완료: `infra/aws-dev/README.md`에 runtime env 흐름, 기존 SSM 값 재설정, 새 backend env 추가, frontend `NEXT_PUBLIC_*` build-time env 추가, GitHub repository variable 수정, 팀원별 IAM user/SSM 최소권한 정책 절차를 문서화했다. 공유 IAM user/access key를 금지하고 사람별 profile 사용을 안내했다.

검증: 문서 전용 변경이므로 AWS apply/deploy는 실행하지 않았다. `git diff --check`와 `rg "Runtime Environment Operations|Reset An Existing SSM Value|Add A Backend Runtime Env Variable|Team Access For SSM" infra/aws-dev/README.md`로 문서 추가 위치와 whitespace를 확인했다. 공개 API/AsyncAPI 계약 변경은 없음.

## 2026-06-30 18:04 - 환경변수 운영 문서 분리

완료: AWS dev 환경변수/secret 운영 절차를 `infra/aws-dev/runtime-environment.md`로 분리했다. 사람용 절차와 AI agent용 수정 규칙을 같은 문서 안에서 구분했고, `infra/aws-dev/README.md`는 새 문서 링크만 남기도록 축약했다. `infra/INDEX.md`에도 환경변수/secret 변경 라우팅을 추가했다.

검증: 문서 전용 변경이므로 AWS apply/deploy는 실행하지 않았다. `git diff --check`, `rg "runtime-environment|Human Operator Guide|AI Agent Guide" infra`로 라우팅과 섹션 존재를 확인했다. 공개 API/AsyncAPI 계약 변경은 없음.
