# 2026-06 Worklog

## 2026-06-26 16:20 - 지식 구조 분석/고급 인사이트 v1 구현

완료: `POST/GET /api/v1/ai/clusters`, `POST/GET /api/v1/ai/insight-reports`를 sync-complete job/report로 구현했다. `NoteProjection` searchable note card source, JPA job/report 저장소, Kafka/NoOp event adapter, usage featureId(`ai-clustering-chat`, `insight-report-chat`)를 연결했다.

아티팩트: `src/main/java/com/brainx/intelligence/clustering`, `src/main/java/com/brainx/intelligence/insight`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/clustering`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/insight`, `docs/technical/knowledge-structure-analysis.md`, `docs/technical/insight-reports.md`

검증: clustering/insight/controller/persistence/event targeted tests, `.\gradlew.bat --no-daemon check --console=plain`, `.\gradlew.bat --no-daemon compileDevUiJava --console=plain`, `git diff --check`를 실행해 통과했다.

## 2026-06-18 11:21 - 기본 문서화

완료: `agent-documentation-compounder` 지침에 맞춰 root agent guide, context vault index, Intelligence Service API 사양 가이드, 검증 workflow, durable improvement index, README를 추가했다.

아티팩트: `AGENTS.md`, `README.md`, `vaults/INDEX.md`, `vaults/agents/intelligence-service.md`, `vaults/workflows/verification.md`, `vaults/durable/INDEX.md`

검증: 문서 전용 변경이므로 Gradle test는 생략했다. `rg "vaults/|AGENTS.md|knowledge-intelligence.openapi.yaml" README.md AGENTS.md vaults`로 문서 링크와 routing path를 확인했다.

## 2026-06-18 11:35 - 도메인 구현 순서 문서화

완료: API 명세 기준 기능 구현 순서를 기술 관점이 아닌 도메인 TODO list로 문서화하고, context vault index에서 라우팅되도록 연결했다.

아티팩트: `vaults/agents/domain-implementation-order.md`, `vaults/INDEX.md`

검증: 문서 전용 변경이므로 Gradle test는 생략했다. `rg "domain-implementation-order|AI 사용 준비|기능 구현 순서" vaults`로 문서 링크와 내용 존재 여부를 확인했다.

## 2026-06-18 11:38 - 도메인 흐름 문서화

완료: API 명세 기준 기능 범위를 도메인 스토리텔링, 이벤트 스토밍 Process Matrix, 컨텍스트 맵 형태의 human-facing domain documentation으로 정리했다. README와 context vault index에서 문서 위치를 찾을 수 있게 연결했다.

아티팩트: `docs/domain/README.md`, `docs/domain/knowledge-intelligence-domain-flow.md`, `README.md`, `vaults/INDEX.md`

검증: 문서 전용 변경이므로 Gradle test는 생략했다. `rg "Domain Storytelling|Event Storming|Context Map|knowledge-intelligence-domain-flow" README.md docs vaults`로 문서 링크와 주요 섹션 존재 여부를 확인했다.

## 2026-06-18 11:52 - 도메인 중심 스캐폴드 생성

완료: 도메인 모델링과 기능 구현 순서 문서를 기준으로 `settings`, `exploration`, `assist`, `chat`, `connection`, `organization`, `clustering`, `insight` 기능별 `domain`, `application.usecase`, `application.port.inbound`, `application.port.outbound` package scaffold를 main/test 양쪽에 추가했다. 기존 context load test package를 `com.brainx.intelligence`로 정리하고, 빈 서비스 상태에서 datasource 없이 context test가 통과하도록 테스트 자동설정만 제외했다.

아티팩트: `src/main/java/com/brainx/intelligence/**/package-info.java`, `src/test/java/com/brainx/intelligence/**/package-info.java`, `src/test/java/com/brainx/intelligence/IntelligenceServiceApplicationTests.java`

검증: `rg "package com\\.brainx\\.intelligence" src/main/java src/test/java`로 package 경로를 확인했다. `.\gradlew.bat test` 실행 결과 BUILD SUCCESSFUL.

## 2026-06-18 12:07 - Spring AI/JPA 인프라 포트 스캐폴드

완료: Spring AI 1.1.8 BOM과 OpenAI starter, H2 test runtime을 추가했다. PostgreSQL/JPA 기본 설정과 H2 test profile을 구성하고, context load test를 `test` profile 기반으로 전환했다. 공통 outbound port(`AiChatPort`, `AiEmbeddingPort`, `EntitlementPort`, `TokenUsagePort`, `WorkspaceNotePort`)와 빈 infrastructure adapter skeleton(`SpringAiAdapter`, `JpaPersistenceAdapter`)을 추가했다.

아티팩트: `build.gradle`, `src/main/resources/application.yaml`, `src/test/resources/application-test.yaml`, `src/main/java/com/brainx/intelligence/shared/application/port/outbound`, `src/main/java/com/brainx/intelligence/infrastructure`, `src/test/java/com/brainx/intelligence/IntelligenceServiceApplicationTests.java`

검증: `rg "spring-ai|h2|application-test|ActiveProfiles" build.gradle src`, `rg "interface .*Port|infrastructure" src/main/java`, `rg "OpenAi.*AutoConfiguration|SPRING_AI_MODEL|OPENAI_API_KEY|jdbc:h2|PostgreSQL" src/main/resources src/test/resources`로 설정과 포트/인프라 위치를 확인했다. `.\gradlew.bat test` 실행 결과 BUILD SUCCESSFUL.

## 2026-06-18 12:45 - AI 사용 준비 도메인/유스케이스 구현

완료: `settings` 기능에 AI 모델 catalog, 사용자 AI 모델 설정, 문체 프로필 도메인 모델과 inbound/outbound port, application service를 구현했다. 모델 catalog와 사용자 설정/문체 프로필을 JPA/H2 테스트 가능 구조로 저장하도록 settings JPA entity, repository, adapter, JSON Map converter를 추가했다. REST controller, OpenAPI DTO, 인증 연동, 실제 OpenAI 호출은 범위에서 제외했다.

아티팩트: `src/main/java/com/brainx/intelligence/settings`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/settings`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/JsonMapAttributeConverter.java`, `src/test/java/com/brainx/intelligence/settings/application/usecase/AiSettingsServiceTest.java`, `src/test/java/com/brainx/intelligence/infrastructure/persistence/jpa/settings/SettingsJpaAdapterTest.java`

검증: `rg "ListAiModelsUseCase|PutAiModelSettingsUseCase|GetStyleProfileUseCase|PutStyleProfileUseCase|AiSettingsService|SettingsJpaAdapter|AiModelJpaEntity|StyleProfileJpaEntity|JsonMapAttributeConverter" src/main/java src/test/java`, `rg "UnknownAiModelException|catalog|StyleProfile.empty|findSettingsByUserId|findStyleProfileByUserId" src/main/java src/test/java`로 구현 위치를 확인했다. `.\gradlew.bat test` 실행 결과 BUILD SUCCESSFUL.

## 2026-06-18 14:53 - AI 모델 비용 도메인 모델 개선

완료: `AiModel.costPer1kTokens` 단일 필드를 `VendorTokenCost` 도메인 모델로 교체하고, 벤더 input/output token cost를 분리했다. `costInfo` raw map은 `AiPricingPolicy` 도메인 모델과 `AiPricingPolicyView`로 승격했다. JPA `ai_models` 매핑은 `vendor_input_cost_per_1k_tokens`, `vendor_output_cost_per_1k_tokens` 컬럼 구조로 변경했다.

아티팩트: `src/main/java/com/brainx/intelligence/settings/domain/VendorTokenCost.java`, `src/main/java/com/brainx/intelligence/settings/domain/AiPricingPolicy.java`, `src/main/java/com/brainx/intelligence/settings/domain/AiModel.java`, `src/main/java/com/brainx/intelligence/settings/application/port/inbound/ListAiModelsUseCase.java`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/settings/AiModelJpaEntity.java`, `src/test/java/com/brainx/intelligence/settings/domain/*Test.java`

검증: `rg "costPer1kTokens|cost_per_1k_tokens|vendorInputCostPer1kTokens|vendorOutputCostPer1kTokens|VendorTokenCost|AiPricingPolicy" src/main/java src/test/java`로 단일 비용 필드 제거와 새 모델 참조를 확인했다. `.\gradlew.bat test` 실행 결과 BUILD SUCCESSFUL.

## 2026-06-18 15:55 - 프로젝트 문서 규칙 일반화

완료: `agent-documentation-compounder` 지침에 맞춰 Intelligence Service agent guide를 endpoint별 목록 중심에서 일반 구현 규칙 중심으로 정리했다. 외부 서비스/외부 도메인 의존성은 application outbound port와 infrastructure adapter skeleton으로 처리하고, 외부 source of truth를 내부 DB로 임의 소유하지 않는 규칙을 명시했다.

아티팩트: `vaults/agents/intelligence-service.md`, `vaults/INDEX.md`, `README.md`

검증: 문서 전용 변경이므로 Gradle test는 생략했다. `rg "endpoint inventory|Endpoint Inventory|Internal sync|AiModelAvailability|External Dependency Rules|외부 의존성" AGENTS.md vaults README.md docs`, `rg "vaults/agents/intelligence-service.md|knowledge-intelligence.openapi.yaml|vaults/" README.md AGENTS.md vaults`로 stale 표현과 문서 라우팅을 확인했다.

## 2026-06-18 16:37 - StyleProfile 입력 방향 문서화

완료: 문체 설정을 JSON 입력으로 받지 않고 프리셋, 짧은 선택지, 미리보기 기반 UX로 제공한 뒤 구조화된 `StyleProfile`로 정규화하는 방향을 문서화했다. 대화 말투, 작문 문체, 협업 방식을 분리하고 저장 프로필보다 현재 요청의 독자/형식/문체 지시가 우선하는 규칙을 정리했다.

아티팩트: `docs/domain/style-profile-input-direction.md`, `docs/domain/README.md`, `vaults/INDEX.md`, `README.md`

검증: 문서 전용 변경이므로 Gradle test는 생략했다. `rg "style-profile-input-direction|StyleProfile|대화 말투|작문 문체|협업 방식" README.md docs vaults`로 문서 링크와 주요 섹션 존재 여부를 확인했다.

## 2026-06-18 16:46 - 챗봇 말투와 문서 문체 분리 보강

완료: `StyleProfile` 입력 방향 문서에 챗봇 말투(`conversationTone`)와 문서 작성 문체(`writingStyle`)를 반드시 분리해야 하는 이유와 적용 원칙을 보강했다. 기본 UX는 단순한 프리셋으로 시작하되, 고급 설정에서 “AI가 나에게 말하는 방식”과 “AI가 글을 써주는 방식”을 따로 조정하도록 정리했다.

아티팩트: `docs/domain/style-profile-input-direction.md`

검증: 문서 전용 변경이므로 Gradle test는 생략했다. `rg "대화 말투와 문서 작성 말투 분리|conversationTone|writingStyle|AI가 나에게 말하는 방식|AI가 글을 써주는 방식" docs/domain/style-profile-input-direction.md vaults/worklogs/2026-06.md`로 반영 내용을 확인했다.

## 2026-06-18 16:57 - StyleProfile 도메인 모델 분리

완료: `StyleProfile` 도메인 모델을 단일 raw `style` map에서 `ConversationTone`, `WritingStyle`, `AssistanceStyle` 세 값 객체로 분리했다. application command/result도 `conversationTone`, `writingStyle`, `assistanceStyle`로 구조화하고, JPA는 기존 `user_style_profiles.style` JSON 컬럼 안에 세 축 nested object를 저장하도록 변환했다.

아티팩트: `src/main/java/com/brainx/intelligence/settings/domain`, `src/main/java/com/brainx/intelligence/settings/application/port/inbound`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/settings/StyleProfileJpaEntity.java`, `src/test/java/com/brainx/intelligence/settings/domain/StyleProfileTest.java`, `docs/domain/style-profile-input-direction.md`

검증: `.\gradlew.bat test` 실행 결과 BUILD SUCCESSFUL. 구용어 검색과 새 명칭 검색으로 구용어 제거 및 새 명칭 반영을 확인했다.

## 2026-06-18 17:32 - Intelligence 관련 외부 계약 슬라이스 추가

완료: 상위 `contracts-v2`에서 Intelligence Service가 consumer인 OpenAPI 내부 호출과 AsyncAPI에서 Intelligence 역할(`AI-Service`)이 producer 또는 consumer로 참여하는 channel을 추출해 프로젝트 계약 디렉터리에 정리했다. 기존 provider OpenAPI는 덮어쓰지 않고, consumed OpenAPI와 AsyncAPI를 별도 파일로 추가했다.

아티팩트: `scripts/extract_intelligence_related_contracts.py`, `src/main/resources/contracts/knowledge-intelligence.consumed.openapi.yaml`, `src/main/resources/contracts/knowledge-intelligence.asyncapi.yaml`, `src/main/resources/contracts/README.md`, `README.md`, `vaults/INDEX.md`, `vaults/agents/intelligence-service.md`, `vaults/workflows/verification.md`

검증: `python scripts\extract_intelligence_related_contracts.py` 실행 결과 consumed OpenAPI 3 operations, AsyncAPI 26 channels/26 operations 추출을 확인했다. `python` YAML parse check로 `src/main/resources/contracts/*.yaml` 로딩을 확인했고, `rg "vaults/|AGENTS.md|knowledge-intelligence.openapi.yaml|knowledge-intelligence.asyncapi.yaml|knowledge-intelligence.consumed.openapi.yaml" README.md AGENTS.md vaults src/main/resources/contracts/README.md`로 문서 라우팅을 확인했다. `.\gradlew.bat test` 실행 결과 BUILD SUCCESSFUL.

## 2026-06-19 09:34 - Swagger UI 실행 설정 추가

완료: `springdoc-openapi-starter-webmvc-ui`를 추가해 Spring MVC controller 기반 Swagger UI와 `/v3/api-docs`를 노출했다. 로컬에서 Postgres 없이 바로 확인할 수 있도록 `local` profile H2 datasource 설정을 추가하고 README에 실행 URL과 command를 기록했다.

아티팩트: `build.gradle`, `src/main/resources/application-local.yaml`, `src/test/java/com/brainx/intelligence/IntelligenceServiceApplicationTests.java`, `README.md`

검증: `.\gradlew.bat test` 실행 결과 BUILD SUCCESSFUL. `.\gradlew.bat bootRun --args=--spring.profiles.active=local`로 로컬 서버를 띄운 뒤 `http://localhost:8080/swagger-ui.html` 응답 `200`, `http://localhost:8080/v3/api-docs` 응답 `200`을 확인했다.

## 2026-06-19 10:13 - Local profile API 인증 우회

완료: 기본 profile에서는 `/api/v1/**` 인증 요구를 유지하고, `local` profile에서만 Swagger 테스트 편의를 위해 `/api/v1/**`를 permit 처리했다. local profile에서 인증 principal이 비어 controller가 실패하지 않도록 local development filter를 추가했다. Swagger 실행 중 DevTools restart가 테스트 산출물 변경을 감지해 서버를 재시작하지 않도록 local profile에서 restart를 비활성화했다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/security/SecurityConfig.java`, `src/main/resources/application-local.yaml`, `src/test/java/com/brainx/intelligence/SettingsControllerDefaultSecurityTest.java`, `src/test/java/com/brainx/intelligence/SettingsControllerLocalSecurityTest.java`, `README.md`

검증: `.\gradlew.bat --no-daemon cleanTest test --tests com.brainx.intelligence.SettingsControllerLocalSecurityTest --tests com.brainx.intelligence.SettingsControllerDefaultSecurityTest` 실행 결과 BUILD SUCCESSFUL. 기본 profile은 인증 없이 `401`, `local` profile은 인증 없이 `200`을 확인했다. `.\gradlew.bat bootRun --args=--spring.profiles.active=local` 실행 후 `http://localhost:8080/swagger-ui.html` 응답 `200`, 인증 없이 `http://localhost:8080/api/v1/ai/models` 응답 `200`을 확인했다.

## 2026-06-18 17:29 - Settings REST API 구현

완료: 현재 settings 도메인 모델 기준으로 OpenAPI 계약을 갱신하고 settings REST controller를 추가했다. `GET /api/v1/ai/models`, `PUT /api/v1/ai/model-settings`, `GET /api/v1/users/me/style-profile`, `PUT /api/v1/users/me/style-profile`를 구현했으며, success/error wrapper와 `/api/v1/**` 인증 요구를 위한 최소 SecurityConfig를 추가했다.

아티팩트: `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/settings/adapter/web/SettingsController.java`, `src/main/java/com/brainx/intelligence/infrastructure/web`, `src/main/java/com/brainx/intelligence/infrastructure/security/SecurityConfig.java`, `src/test/java/com/brainx/intelligence/settings/adapter/web/SettingsControllerTest.java`

검증: `.\gradlew.bat test --no-daemon` 실행 결과 BUILD SUCCESSFUL. `rg "costPer1kTokens|vendorInputCostPer1kTokens|vendorOutputCostPer1kTokens|conversationTone|writingStyle|assistanceStyle" src/main/resources/contracts src/main/java src/test/java`, `rg "@RestController|SecurityFilterChain|ApiSuccessResponse|ApiErrorResponse|SettingsControllerTest" src/main/java src/test/java`로 OpenAPI와 web adapter 반영을 확인했다.

## 2026-06-18 17:35 - OpenAPI SSOT 동기화 규칙 반영

완료: 공개 API 계약 변경 시 로컬 추출본이 아니라 `../../contracts-v2/brainx-openapi.ssot.yaml`을 먼저 수정하고 `scripts/extract_intelligence_openapi.py`로 로컬 슬라이스를 재생성해야 한다는 규칙을 문서화했다. Settings API의 vendor token cost와 StyleProfile schema 변경도 SSOT에 반영한 뒤 로컬 `knowledge-intelligence.openapi.yaml`을 재추출했다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `AGENTS.md`, `vaults/agents/intelligence-service.md`, `vaults/workflows/verification.md`

검증: `python scripts\extract_intelligence_openapi.py` 실행 결과 18개 operation, 17개 path 추출 및 component ref 검증 성공. SSOT에 기존 merge conflict marker가 있어 스크립트가 `--conflict-side=right` 기준으로 37개 block을 무시했다는 경고를 출력했다. `costPer1kTokens`가 SSOT/로컬 슬라이스에서 제거됐고, `vendorInputCostPer1kTokens`, `vendorOutputCostPer1kTokens`, `conversationTone`, `writingStyle`, `assistanceStyle`가 양쪽에 반영됐음을 `rg`로 확인했다. `.\gradlew.bat test --no-daemon` 실행 결과 BUILD SUCCESSFUL.

## 2026-06-19 10:35 - Vaadin 도메인 Dev UI 추가

완료: Vaadin 24 기반 개발 전용 domain cockpit을 `devUi` source set으로 분리해 추가했다. `/dev-ui` route에서 `settings`, `exploration`, `assist`, `chat`, `connection`, `organization`, `clustering`, `insight` 도메인을 전환하며, settings는 실제 usecase를 호출하고 나머지는 fixture preview와 delta stream preview로 확인한다. `bootRunDevUi` task는 `dev-ui` profile, H2 datasource, devtools restart 비활성화, dev 전용 JPA scan 설정을 사용한다. production `bootJar`에는 dev UI와 Vaadin runtime이 포함되지 않도록 분리했고, Vaadin dev mode generated frontend는 `.gitignore`에 제외했다.

아티팩트: `build.gradle`, `.gitignore`, `src/devUi/java/com/brainx/intelligence/devui`, `src/devUi/resources/application-dev-ui.yaml`, `src/devUi/resources/dev-ui-fixtures`

검증: `.\gradlew.bat --no-daemon check` 실행 결과 BUILD SUCCESSFUL. `.\gradlew.bat --no-daemon compileDevUiJava processDevUiResources bootJar` 실행 결과 BUILD SUCCESSFUL. production jar 내부를 zip API로 검사해 `devui`, `dev-ui`, `vaadin` entry가 없음을 확인했다. 8080 포트가 사용 중이라 `SERVER_PORT=18080`으로 `.\gradlew.bat --no-daemon bootRunDevUi` smoke test를 실행했고 `GET http://localhost:18080/dev-ui` 응답 `200`을 확인했다.

## 2026-06-19 10:57 - Vaadin Dev UI 빈 화면 수정

완료: `/dev-ui` 초기 HTML은 200으로 응답하지만 Vaadin UIDL 요청 `/?v-r=uidl&v-uiId=0`이 CSRF 403으로 차단되어 빈 화면과 `Connection lost` 상태가 뜨던 문제를 수정했다. `dev-ui` profile에서만 CSRF를 비활성화하고, 기본 profile과 local profile의 기존 API 인증 정책은 유지했다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/security/SecurityConfig.java`

검증: Playwright/Chrome으로 `http://localhost:18080/dev-ui`를 열어 domain cockpit 텍스트와 settings 모델 grid 렌더링을 확인했고 UIDL 403이 사라졌음을 확인했다. `.\gradlew.bat --no-daemon check` 실행 결과 BUILD SUCCESSFUL.

## 2026-06-19 10:13 - Java 품질 검증 게이트 추가

완료: Gradle `check`에 Java compile warning gate와 ArchUnit architecture test를 추가했다. Deprecated/unchecked compiler warning은 `-Werror`로 build failure 처리하고, domain/application package dependency, JPA naming package, deprecated Spring Boot mock annotation 사용 금지를 ArchUnit으로 검증한다. 기존 web MVC test와 새 보안 테스트의 `@MockBean` 사용은 `@MockitoBean`으로 교체했고, local no-auth security context의 user id fallback을 보정했다.

아티팩트: `build.gradle`, `src/test/java/com/brainx/intelligence/architecture/ArchitectureTest.java`, `src/test/java/com/brainx/intelligence/settings/adapter/web/SettingsControllerTest.java`, `src/test/java/com/brainx/intelligence/SettingsControllerDefaultSecurityTest.java`, `src/test/java/com/brainx/intelligence/SettingsControllerLocalSecurityTest.java`, `src/main/java/com/brainx/intelligence/infrastructure/security/SecurityConfig.java`, `src/main/java/com/brainx/intelligence/settings/adapter/web/SettingsController.java`, `vaults/workflows/verification.md`

검증: `.\gradlew.bat compileJava compileTestJava test --tests com.brainx.intelligence.architecture.ArchitectureTest --no-daemon` 실행 결과 BUILD SUCCESSFUL. `.\gradlew.bat check --no-daemon --stacktrace` 실행 결과 BUILD SUCCESSFUL. `.\gradlew.bat clean check --no-daemon --stacktrace`는 동시 clean/test 상태에서 test runtime classpath가 비어 `NoClassDefFoundError`와 ArchUnit empty import로 실패했으나, 직전 `check`는 통과했다. `rg "org\.springframework\.boot\.test\.mock\.mockito\.(MockBean|SpyBean)|@(MockBean|SpyBean)" src/test src/main`로 금지 annotation 잔존 사용이 없음을 확인했다.

## 2026-06-19 11:21 - 지식 탐색 기본 기능 구현

완료: `exploration` 도메인에 시맨틱 검색과 노트 요약 조회 vertical slice를 추가했다. `POST /api/v1/intelligence/semantic-search`는 권한 확인, deterministic embedding, JPA read model 검색, token usage/event 포트 호출 후 OpenAPI wrapper로 결과를 반환한다. `GET /api/v1/notes/{noteId}/summary`는 저장된 AI 요약을 우선 반환하고, cache miss 시 Workspace snapshot에서 excerpt summary를 반환한다. 실제 Kafka/Workspace/Entitlement 연동은 no-op 또는 neutral adapter skeleton으로 두어 로컬 실행과 context load가 깨지지 않게 했다.

아티팩트: `src/main/java/com/brainx/intelligence/exploration`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/exploration`, `src/main/java/com/brainx/intelligence/infrastructure/events/NoOpIntelligenceEventAdapter.java`, `src/main/java/com/brainx/intelligence/infrastructure/entitlement/ExternalEntitlementAdapter.java`, `src/main/java/com/brainx/intelligence/infrastructure/workspace/ExternalWorkspaceNoteAdapter.java`, `src/main/java/com/brainx/intelligence/shared/application/port/outbound/TokenUsagePort.java`, `src/test/java/com/brainx/intelligence/exploration`, `src/test/java/com/brainx/intelligence/infrastructure/persistence/jpa/exploration`

검증: `.\gradlew.bat --no-daemon compileJava compileTestJava`, 신규 exploration 테스트 대상 `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.exploration.domain.ExplorationDomainTest --tests com.brainx.intelligence.exploration.application.usecase.ExplorationServiceTest --tests com.brainx.intelligence.infrastructure.persistence.jpa.exploration.ExplorationJpaAdapterTest --tests com.brainx.intelligence.exploration.adapter.web.ExplorationControllerTest`, 전체 `.\gradlew.bat --no-daemon test`, `.\gradlew.bat --no-daemon check` 모두 BUILD SUCCESSFUL. `.\gradlew.bat --no-daemon clean check`는 기존 `bootRunDevUi` 프로세스가 `build/dev-ui-server/*.log` 파일을 열고 있어 `clean` 단계에서만 실패했으며, 부분 clean 이후 재실행한 `.\gradlew.bat --no-daemon check`는 BUILD SUCCESSFUL.

## 2026-06-19 11:39 - Qdrant vector DB 인프라 추가

완료: 로컬 개발용 `compose.yaml`에 Qdrant 단일 노드 서비스를 추가하고, Spring AI Qdrant VectorStore starter와 `spring.ai.vectorstore.qdrant.*` 설정을 추가했다. `NoteSearchIndexPort`는 유지하면서 `QdrantNoteSearchIndexAdapter`가 Spring AI `VectorStore`로 저장/검색을 처리하도록 추가했고, Qdrant/EmbeddingModel 미설정 환경에서는 `NoOpNoteSearchIndexAdapter`가 context load를 유지하게 했다. 기존 `ExplorationJpaAdapter`는 note summary JPA 저장소 역할만 남기고 JPA JSON embedding 검색 entity/repository/converter는 제거했다.

아티팩트: `compose.yaml`, `build.gradle`, `src/main/resources/application.yaml`, `src/test/resources/application-test.yaml`, `src/devUi/resources/application-dev-ui.yaml`, `src/main/java/com/brainx/intelligence/infrastructure/vector`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/exploration/ExplorationJpaAdapter.java`, `src/test/java/com/brainx/intelligence/infrastructure/vector/qdrant/QdrantNoteSearchIndexAdapterTest.java`

검증: `.\gradlew.bat --no-daemon compileJava compileTestJava`, `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.vector.qdrant.QdrantNoteSearchIndexAdapterTest --tests com.brainx.intelligence.infrastructure.persistence.jpa.exploration.ExplorationJpaAdapterTest`, `.\gradlew.bat --no-daemon test`, `.\gradlew.bat --no-daemon check`, `.\gradlew.bat --no-daemon clean check` 모두 BUILD SUCCESSFUL. `docker compose config`로 compose 문법과 Qdrant port/volume/healthcheck 구성을 확인했다.

## 2026-06-19 12:01 - ConditionalOnBean 기술 문서 추가

완료: Spring Boot `@ConditionalOnBean`의 의미, 동작 방식, 일반 `@Component` 사용 시 주의점, Qdrant `VectorStore` adapter 적용 이유를 기술 문서로 정리했다. `docs/domain`은 도메인 문서 전용이므로 새 `docs/technical` 디렉터리에 문서를 두고 README와 vault index에서 라우팅했다.

아티팩트: `docs/technical/README.md`, `docs/technical/conditional-on-bean.md`, `README.md`, `vaults/INDEX.md`

검증: 문서 전용 변경이므로 Gradle 검증은 생략했다. `rg "conditional-on-bean|ConditionalOnBean|QdrantNoteSearchIndexAdapter|NoOpNoteSearchIndexAdapter|기술 문서" README.md docs vaults`로 문서 링크와 주요 내용 존재 여부를 확인했다.

## 2026-06-19 12:30 - Semantic search VectorStore 방식 통일

완료: `exploration` semantic search 흐름에서 `AiEmbeddingPort` 직접 호출과 raw embedding 전달을 제거하고, query text를 `NoteSearchIndexPort`로 넘겨 Qdrant/Spring AI `VectorStore` adapter가 embedding 생성과 vector search를 담당하도록 책임을 통일했다. `NoteSearchQuery`와 `NoteSearchDocument`에서도 embedding vector 필드를 제거해 application/domain 경계가 vector 저장 방식에 의존하지 않게 했다.

아티팩트: `src/main/java/com/brainx/intelligence/exploration/application/usecase/ExplorationService.java`, `src/main/java/com/brainx/intelligence/exploration/application/port/outbound/NoteSearchIndexPort.java`, `src/main/java/com/brainx/intelligence/exploration/domain/NoteSearchDocument.java`, `src/test/java/com/brainx/intelligence/exploration/application/usecase/ExplorationServiceTest.java`, `src/test/java/com/brainx/intelligence/infrastructure/vector/qdrant/QdrantNoteSearchIndexAdapterTest.java`

검증: `.\gradlew.bat --no-daemon compileJava compileTestJava`, `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.exploration.application.usecase.ExplorationServiceTest --tests com.brainx.intelligence.infrastructure.vector.qdrant.QdrantNoteSearchIndexAdapterTest`, `.\gradlew.bat --no-daemon test`, `.\gradlew.bat --no-daemon check` 모두 BUILD SUCCESSFUL. `rg "AiEmbeddingPort|queryEmbedding|List<Double> embedding|embeddingRequests" src/main/java/com/brainx/intelligence/exploration src/test/java/com/brainx/intelligence/exploration src/main/java/com/brainx/intelligence/infrastructure/vector src/test/java/com/brainx/intelligence/infrastructure/vector`로 exploration/vector 경로의 이전 raw embedding 사용이 없음을 확인했다.

## 2026-06-19 12:33 - VectorStore embedding model 지정 문서화

완료: Spring AI Qdrant `VectorStore`가 사용할 embedding model을 환경변수로 지정하는 방법을 기술 문서로 추가했다. `SPRING_AI_MODEL_EMBEDDING=openai`, `OPENAI_API_KEY`, `SPRING_AI_OPENAI_EMBEDDING_OPTIONS_MODEL`, Qdrant gRPC port와 collection 설정의 의미를 정리하고, `VectorStore.add(...)`와 `VectorStore.similaritySearch(...)`에서 embedding이 처리된다는 책임 경계를 명시했다.

아티팩트: `docs/technical/vectorstore-embedding-model.md`, `docs/technical/README.md`, `README.md`, `vaults/INDEX.md`

검증: 문서 전용 변경이므로 Gradle 검증은 생략했다. `vaults/workflows/verification.md`의 documentation check 기준을 확인했고, `rg "vectorstore-embedding-model|SPRING_AI_MODEL_EMBEDDING|SPRING_AI_OPENAI_EMBEDDING_OPTIONS_MODEL|QDRANT_GRPC_PORT|VectorStore.add|similaritySearch|embedding model" README.md docs vaults src/main/resources/application.yaml`로 문서 라우팅과 주요 설정명 존재를 확인했다.

## 2026-06-19 14:42 - Voyage AI 내부 embedding adapter 추가

완료: Voyage AI를 public API로 노출하지 않고 `infrastructure.ai.voyage` 내부 adapter로 연동했다. `BRAINX_AI_EMBEDDING_PROVIDER=voyage`일 때 `VoyageEmbeddingModel`을 Spring AI `EmbeddingModel` bean으로 등록해 Qdrant `VectorStore`가 사용하게 했고, 저장용 document embedding은 `input_type=document`, 검색 query embedding은 `input_type=query`로 분리했다. 기존 `SpringAiAdapter`의 deterministic fallback은 제거하고 등록된 `EmbeddingModel`에 위임하도록 정리했다. 실제 API key는 repository 파일에 저장하지 않았다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/ai/voyage`, `src/main/java/com/brainx/intelligence/infrastructure/ai/SpringAiAdapter.java`, `src/main/resources/application.yaml`, `src/test/java/com/brainx/intelligence/infrastructure/ai`, `docs/technical/vectorstore-embedding-model.md`

검증: `.\gradlew.bat --no-daemon compileJava compileTestJava`, `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.ai.voyage.VoyageEmbeddingModelTest --tests com.brainx.intelligence.infrastructure.ai.voyage.VoyageEmbeddingConfigurationTest --tests com.brainx.intelligence.infrastructure.ai.SpringAiAdapterTest`, `.\gradlew.bat --no-daemon test`, `.\gradlew.bat --no-daemon check` 모두 BUILD SUCCESSFUL. secret literal과 `VOYAGE_API_KEY` 직접 할당 패턴 검색으로 제공된 API key가 파일에 저장되지 않았음을 확인했다.

## 2026-06-19 15:34 - Voyage local secret properties 추가

완료: 사용자가 제공한 Voyage API key를 project root의 `.brainx-local.properties`에 저장하고, 이 파일을 `.gitignore`에 추가해 repository 추적 대상에서 제외했다. `local` profile에서만 `optional:file:./.brainx-local.properties`를 import하도록 `application-local.yaml`에 연결했으며, 같은 파일에 provider `voyage`와 Voyage 1024차원용 Qdrant collection 이름도 함께 지정했다.

아티팩트: `.brainx-local.properties`, `.gitignore`, `src/main/resources/application-local.yaml`, `docs/technical/vectorstore-embedding-model.md`

검증: `git check-ignore -v .brainx-local.properties`로 local secret file이 ignore되는지 확인했다. `.\gradlew.bat --no-daemon test`, `.\gradlew.bat --no-daemon check` 모두 BUILD SUCCESSFUL.

## 2026-06-19 15:41 - AI-Service consumer 이벤트 도메인 매핑 문서화

완료: AsyncAPI 계약 기준으로 `AI-Service`가 consumer로 받는 이벤트를 도메인 기능별로 정리했다. Workspace 노트/폴더 이벤트, Intelligence 내부 작업 이벤트, 외부 도메인 이벤트를 나누고, 각 이벤트가 지식 탐색, RAG 채팅, 작성 보조, 연결 추천, 정리 제안, 구조 분석, 인사이트, 사용자 데이터 삭제 흐름에 어떤 도메인 반응을 일으키는지 설명했다. 현재 구현에는 consumer 자동 반영 흐름이 모두 붙어 있지 않다는 차이도 명시했다.

아티팩트: `docs/domain/consumed-events-domain-map.md`, `docs/domain/README.md`, `README.md`, `vaults/INDEX.md`

검증: 문서 전용 변경이므로 Gradle 검증은 생략했다. `rg "consumed-events-domain-map|AI-Service가 consumer|NoteContentSaved|NoteCreated|UserDeletionRequested|CaptureReceived|ClusterJobRequested|InsightReportRequested|UserStyleProfileChanged|eventId" README.md docs vaults src/main/resources/contracts/knowledge-intelligence.asyncapi.yaml`와 `git diff --check -- docs/domain/consumed-events-domain-map.md docs/domain/README.md README.md vaults/INDEX.md`를 실행해 문서 라우팅, 주요 이벤트명, whitespace를 확인했다.

## 2026-06-19 15:46 - Consumer 이벤트 구현 체크포인트 문서화

완료: `AI-Service`가 consumer로 받는 이벤트별 구현 체크포인트 문서를 추가했다. 공통 idempotency/DLQ/logging 원칙과 함께 `NoteCreated`, `NoteContentSaved`, 삭제/이동/링크/폴더 이벤트, 내부 작업 이벤트, `CaptureReceived`, `UserDeletionRequested`별 projection, embedding, vector index, cache invalidation, job 처리 체크포인트를 정리했다. 특히 `NoteCreated`는 payload에 markdown이 없으므로 full embedding을 위해 Workspace snapshot 조회가 필요하고, snapshot이 없으면 title-only provisional index 또는 `NoteContentSaved` 대기를 선택해야 한다고 명시했다.

아티팩트: `docs/technical/consumed-events-implementation-checkpoints.md`, `docs/technical/README.md`, `docs/domain/consumed-events-domain-map.md`, `README.md`, `vaults/INDEX.md`

검증: 문서 전용 변경이므로 Gradle 검증은 생략했다. `rg "consumed-events-implementation-checkpoints|NoteCreated|NoteContentSaved|NoteDeleted|ClusterJobRequested|InsightReportRequested|UserDeletionRequested|Workspace snapshot|NoteSearchIndexPort|\- \[ \]" README.md docs vaults/INDEX.md`와 `git diff --check -- docs/technical/consumed-events-implementation-checkpoints.md docs/technical/README.md docs/domain/consumed-events-domain-map.md README.md vaults/INDEX.md`를 실행해 문서 라우팅, 주요 이벤트별 체크포인트, whitespace를 확인했다.

## 2026-06-19 16:39 - Snapshot 기반 note consumer 구현

완료: 공통 event envelope/dispatcher, `eventId` idempotency 및 failed-event JPA store, 기본 비활성 Kafka listener를 추가했다. `intelligence_note_projections` JPA read model을 만들고 `NoteCreated`, `NoteContentSaved`, `NoteMetadataChanged`, `NoteTagsChanged`, `NoteTrashed`, `NoteDeleted`, `NotesMoved` handler를 연결했다. 노트 본문은 이벤트에 싣지 않고 `WorkspaceNotePort`가 명세의 `/internal/v1/workspace/notes/{noteId}/snapshot`을 호출해 가져오며, Qdrant index는 note 단위 upsert/delete를 수행한다. summary cache는 본문 변경/삭제 시 삭제하도록 확장했다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/events/consumer`, `src/main/java/com/brainx/intelligence/infrastructure/events/note`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/events`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/note`, `src/main/java/com/brainx/intelligence/infrastructure/workspace`, `docs/technical/consumed-events-implementation-checkpoints.md`, `docs/domain/consumed-events-domain-map.md`

검증: `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.infrastructure.events.consumer.BrainxEventDispatcherTest" --tests "com.brainx.intelligence.infrastructure.events.note.WorkspaceNoteEventHandlerTest" --tests "com.brainx.intelligence.infrastructure.workspace.ExternalWorkspaceNoteAdapterTest" --tests "com.brainx.intelligence.infrastructure.vector.qdrant.QdrantNoteSearchIndexAdapterTest"` 통과. `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.infrastructure.persistence.jpa.events.EventConsumptionJpaAdapterTest" --tests "com.brainx.intelligence.infrastructure.persistence.jpa.note.NoteProjectionJpaAdapterTest" --tests "com.brainx.intelligence.infrastructure.persistence.jpa.exploration.ExplorationJpaAdapterTest"` 통과. `.\gradlew.bat --no-daemon test`, `.\gradlew.bat --no-daemon check`, `git diff --check` 모두 BUILD SUCCESSFUL.

## 2026-06-19 16:58 - Note chunk vector index 구현

완료: `NoteSearchDocument`를 chunk-aware 모델로 확장하고, `NoteSearchIndexPort`에 `replaceNoteChunks`를 추가했다. `MarkdownNoteChunker`는 heading/paragraph 경계를 우선 보존하고 긴 block은 1200자/150자 overlap으로 나누며 최대 80개 chunk를 만든다. `WorkspaceNoteEventHandler`는 snapshot markdown을 chunk 집합으로 바꿔 Qdrant index를 note 단위로 교체하고, `NoteCreated` snapshot miss는 title-only chunk를 저장한다. Qdrant adapter는 chunk metadata를 저장하며, semantic search 결과는 같은 `noteId`의 chunk 중 최고 score만 반환하도록 dedupe한다.

아티팩트: `src/main/java/com/brainx/intelligence/exploration/domain/NoteSearchDocument.java`, `src/main/java/com/brainx/intelligence/exploration/application/port/outbound/NoteSearchIndexPort.java`, `src/main/java/com/brainx/intelligence/infrastructure/events/note/MarkdownNoteChunker.java`, `src/main/java/com/brainx/intelligence/infrastructure/events/note/WorkspaceNoteEventHandler.java`, `src/main/java/com/brainx/intelligence/infrastructure/vector/qdrant/QdrantNoteSearchIndexAdapter.java`, `docs/technical/consumed-events-implementation-checkpoints.md`

검증: `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.infrastructure.events.note.MarkdownNoteChunkerTest" --tests "com.brainx.intelligence.infrastructure.events.note.WorkspaceNoteEventHandlerTest" --tests "com.brainx.intelligence.infrastructure.vector.qdrant.QdrantNoteSearchIndexAdapterTest"` 통과. `.\gradlew.bat --no-daemon test`, `.\gradlew.bat --no-daemon check`, `git diff --check` 모두 BUILD SUCCESSFUL.

## 2026-06-19 17:05 - Note chunking 설명 문서 추가

완료: `MarkdownNoteChunker`의 입력/출력, 기본값, markdown block 처리 순서, title prefix 정책, Qdrant 교체 흐름, semantic search note-level dedupe 정책, 현재 한계를 정리한 기술 문서를 추가했다. README, technical README, vault index에 라우팅을 연결했다.

아티팩트: `docs/technical/note-chunking.md`, `docs/technical/README.md`, `README.md`, `vaults/INDEX.md`

검증: 문서 전용 변경이므로 Gradle 검증은 생략했다. `git diff --check -- docs/technical/note-chunking.md docs/technical/README.md README.md vaults/INDEX.md`와 `rg "note-chunking|MarkdownNoteChunker|chunkId|replaceNoteChunks|semantic search dedupe|노트 markdown" README.md docs/technical vaults/INDEX.md`를 실행해 whitespace와 문서 라우팅을 확인했다.

## 2026-06-19 17:33 - sample_notes RAG CLI 및 OpenAI chat 설정

완료: `sample_notes/*.md`를 Workspace snapshot처럼 읽어 note projection과 Qdrant chunk index에 저장하는 개발 전용 CLI runner를 추가했다. RAG 질의는 `NoteChunkRetrievalPort`로 note-level dedupe 전 chunk hit를 조회하고, `ChatClient`가 있으면 `AiChatPort.generate`로 `gpt-5.4-mini` 답변을 생성하며 없으면 retrieval-only JSON을 반환한다. local ignored secret 파일에는 OpenAI chat 설정을 추가했고, secret 값은 tracked file과 로그에 남기지 않았다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/dev/rag`, `src/main/java/com/brainx/intelligence/exploration/application/port/outbound/NoteChunkRetrievalPort.java`, `src/main/java/com/brainx/intelligence/exploration/domain/NoteChunkSearchResult.java`, `src/main/java/com/brainx/intelligence/infrastructure/ai/SpringAiAdapter.java`, `src/main/java/com/brainx/intelligence/infrastructure/vector/qdrant/QdrantNoteSearchIndexAdapter.java`, `docs/technical/sample-notes-rag-cli.md`, `src/main/resources/application.yaml`

검증: `.\gradlew.bat --no-daemon compileJava compileTestJava`, 신규/수정 테스트 대상 `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.dev.rag.SampleNoteLoaderTest --tests com.brainx.intelligence.infrastructure.dev.rag.SampleRagServiceTest --tests com.brainx.intelligence.infrastructure.ai.SpringAiAdapterTest --tests com.brainx.intelligence.infrastructure.vector.qdrant.QdrantNoteSearchIndexAdapterTest`, 전체 `.\gradlew.bat --no-daemon test`, `.\gradlew.bat --no-daemon check`, `git diff --check` 모두 BUILD SUCCESSFUL. tracked diff에 실제 OpenAI key prefix가 없는 것도 확인했다.

## 2026-06-19 17:43 - 실제 API key smoke 후 Qdrant adapter 보정

완료: 실제 OpenAI/Voyage key로 provider smoke를 실행해 OpenAI `gpt-5.4-mini` chat completion과 Voyage `voyage-4-lite` 1024차원 embedding 응답을 확인했다. 애플리케이션 RAG CLI smoke 중 `@ConditionalOnBean` 처리 순서 때문에 Qdrant adapter 대신 no-op path가 쓰이는 문제를 확인하고, adapter가 `ObjectProvider<VectorStore>`를 통해 런타임에 VectorStore를 resolve하도록 보정했다. 추가로 Spring AI Qdrant store가 `Document.id`를 UUID로 파싱한다는 제약을 확인해 Qdrant 내부 document id를 deterministic UUID로 바꾸고 logical chunk id는 metadata `chunkId`로 유지했다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/vector/qdrant/QdrantNoteSearchIndexAdapter.java`, `src/test/java/com/brainx/intelligence/infrastructure/vector/qdrant/QdrantNoteSearchIndexAdapterTest.java`, `docs/technical/note-chunking.md`

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.vector.qdrant.QdrantNoteSearchIndexAdapterTest --tests com.brainx.intelligence.infrastructure.dev.rag.SampleRagServiceTest` 통과. provider smoke는 OpenAI model list/chat completion과 Voyage embedding endpoint가 모두 성공했다. secret 값은 출력하거나 tracked file에 남기지 않았다.

## 2026-06-19 17:51 - sample_notes RAG fixture 추적

완료: 로컬 RAG 품질 테스트에 쓰던 `sample_notes` markdown fixture 21개를 repository 추적 대상으로 추가했다. BrainX API/도메인/노트/작성/아키텍처 관련 sample 문서를 실제 chunking과 retrieval 품질 확인용 corpus로 사용할 수 있다.

아티팩트: `sample_notes`

검증: `rg`로 실제 OpenAI/Voyage key prefix와 API key 문자열을 검사했고, fixture 내부에는 API 명세 예시용 password 문자열만 있음을 확인했다. `git diff --cached --check`는 sample markdown의 trailing whitespace를 보고했지만, 원본 fixture의 hard-break와 입력 품질을 보존하기 위해 정규화하지 않았다.

## 2026-06-22 10:15 - sample_notes RAG ingest 재실행

완료: local ignored secret 설정에 Voyage embedding key를 갱신하고 Qdrant `brainx_note_search_voyage_1024` collection에 `sample_notes` 21개 노트를 재색인했다. 이전 smoke test 잔여 point 1개를 삭제해 현재 sample corpus와 Qdrant point 수를 맞췄다. secret 값은 worklog와 tracked file에 기록하지 않았다.

아티팩트: `.brainx-local.properties` local ignored file, Qdrant `brainx_note_search_voyage_1024` collection

검증: `.\gradlew.bat --no-daemon bootRun --args="--spring.profiles.active=local --brainx.dev.sample-rag.enabled=true --brainx.dev.sample-rag.command=ingest"` 실행 결과 `notesIndexed=21`, `chunksIndexed=211`. Qdrant `/points/count`에서 `sample-user` count `211`, collection status `green`을 확인했다. retrieval-only `ask` smoke는 ChatClient auto-configuration을 제외하고 실행해 chunk context 반환을 확인했다. 코드 변경은 없어 Gradle `check`는 생략했다.

## 2026-06-22 10:36 - RAG CLI 질의 출력 capture script 추가

완료: 기존 Java/Spring RAG 구현은 수정하지 않고 `sample_notes` RAG CLI를 여러 질의로 반복 실행해 질의별 원본 stdout/stderr와 파싱된 JSON 응답을 저장하는 Python script를 추가했다. 점수 계산 없이 어떤 질의에 어떤 출력이 나왔는지만 `build/rag-captures/<run-id>/summary.json`, `responses.jsonl`, `raw/*.txt`에 남긴다.

아티팩트: `scripts/capture_sample_rag_outputs.py`

검증: `python -m py_compile scripts\capture_sample_rag_outputs.py`, `python scripts\capture_sample_rag_outputs.py --help` 통과. Java 코드, Spring 설정, OpenAPI 계약은 변경하지 않아 Gradle `check`는 생략했다.

## 2026-06-22 10:42 - RAG CLI capture script agent 문서화

완료: 다음 AI agent가 `sample_notes` RAG CLI와 질의별 출력 capture script 정보를 바로 찾을 수 있게 `docs/technical/sample-notes-rag-cli.md`에 Agent Quick Reference, capture script 실행법, 저장 파일 구조, `summary.json`/`responses.jsonl` 읽는 법을 추가했다. `docs/technical/README.md`와 `vaults/INDEX.md` 라우팅 설명도 capture script까지 포함하도록 갱신했다.

아티팩트: `docs/technical/sample-notes-rag-cli.md`, `docs/technical/README.md`, `vaults/INDEX.md`

검증: 문서 전용 변경이므로 Gradle 검증은 생략했다. `git diff --check -- docs/technical/sample-notes-rag-cli.md docs/technical/README.md vaults/INDEX.md vaults/worklogs/2026-06.md`와 `rg "capture_sample_rag_outputs|rag-captures|responses.jsonl|Agent Quick Reference|질의별 출력 capture" docs/technical vaults/INDEX.md`로 문서 라우팅과 핵심 키워드를 확인했다.

## 2026-06-22 11:01 - RAG CLI 질의 출력 capture 실제 실행

완료: `python scripts\capture_sample_rag_outputs.py --skip-ingest`로 기본 질의 12개를 실제 실행해 질의별 RAG CLI 출력 JSON과 원본 stdout/stderr를 저장했다. 모든 질의는 `returnCode=0`, `answerMode=llm`, `model=gpt-5.4-mini`, `contextCount=8`로 완료됐다.

아티팩트: `build/rag-captures/20260622-104607/summary.json`, `build/rag-captures/20260622-104607/responses.jsonl`, `build/rag-captures/20260622-104607/raw/*.txt`

검증: `build/rag-captures/20260622-104607/summary.json`을 읽어 12개 run의 `status=completed`, `returnCode=0`, `answerMode=llm`, `contextCount=8`을 확인했다. 이번 실행은 기존 색인을 사용하기 위해 ingest를 건너뛰었다.

## 2026-06-22 11:33 - RAG capture 한글 stdout 인코딩 보정

완료: 이전 capture 결과의 한글이 깨진 원인을 확인했다. Windows에서 Spring Boot app stdout이 CP949 계열 byte로 나오고 Gradle `bootRun` 중계/capture가 UTF-8로 해석하면서 `ä��`, `�` 형태로 손상됐다. capture script가 child process에 `JAVA_TOOL_OPTIONS=-Dfile.encoding=UTF-8 -Dsun.stdout.encoding=UTF-8 -Dsun.stderr.encoding=UTF-8`을 자동으로 넣고, 질의는 `BRAINX_DEV_SAMPLE_RAG_QUERY` 환경변수로 전달하며, 원본 stdout/stderr byte를 `.bin` 파일로 함께 저장하도록 보정했다. 문서에도 UTF-8 강제와 `.bin` 확인 방법을 추가했다.

아티팩트: `scripts/capture_sample_rag_outputs.py`, `docs/technical/sample-notes-rag-cli.md`, `build/rag-captures/20260622-script-utf8-smoke/summary.json`, `build/rag-captures/20260622-script-utf8-smoke/responses.jsonl`

검증: `python -m py_compile scripts\capture_sample_rag_outputs.py`, `python scripts\capture_sample_rag_outputs.py --help` 통과. `python scripts\capture_sample_rag_outputs.py --skip-ingest --run-name 20260622-script-utf8-smoke "RAG 채팅 메시지 전송 API 경로와 응답 방식은?"` 실행 후 `response.query`, `response.answer`, `response.contexts[0].text`의 replacement count가 0이고 한글이 정상 표시됨을 확인했다.

## 2026-06-22 11:54 - RAG capture batch ask 및 retrieval/chat wrapper 분리

완료: 질의마다 `gradlew bootRun`을 반복하던 capture script를 `bootJar` 1회 빌드 후 `PropertiesLauncher`로 `ask` session 1회를 실행하고 여러 query를 stdin으로 처리하는 구조로 바꿨다. 기존 Java/Spring 코드는 수정하지 않았다. retrieval-only wrapper `scripts/capture_sample_rag_retrieval.py`와 chat/LLM wrapper `scripts/capture_sample_rag_chat.py`를 추가했다. retrieval mode는 `SPRING_AI_MODEL_CHAT=none`과 `ChatClientAutoConfiguration` exclude를 적용해 ChatModel 호출 없이 context만 저장한다. local H2 driver는 bootJar에 없어서 Gradle cache의 H2 jar를 `loader.path`에 붙인다.

아티팩트: `scripts/capture_sample_rag_outputs.py`, `scripts/capture_sample_rag_retrieval.py`, `scripts/capture_sample_rag_chat.py`, `docs/technical/sample-notes-rag-cli.md`, `docs/technical/README.md`, `vaults/INDEX.md`, `build/rag-captures/20260622-retrieval-batch-two`, `build/rag-captures/20260622-chat-batch-smoke`

검증: `python -m py_compile scripts\capture_sample_rag_outputs.py scripts\capture_sample_rag_retrieval.py scripts\capture_sample_rag_chat.py`, `python scripts\capture_sample_rag_retrieval.py --help` 통과. `python scripts\capture_sample_rag_retrieval.py --skip-build --skip-ingest --run-name 20260622-retrieval-batch-two "...질의1..." "...질의2..."`로 한 ask session에서 2개 JSON이 저장되고 둘 다 `answerMode=retrieval`, `model=none`, `contextCount=8`임을 확인했다. `python scripts\capture_sample_rag_chat.py --skip-build --skip-ingest --run-name 20260622-chat-batch-smoke "...질의1..."`는 `answerMode=llm`, `model=gpt-5.4-mini`, `contextCount=8`로 완료됐다.

## 2026-06-22 11:56 - RAG retrieval batch 12개 질의 실행

완료: 새 retrieval-only wrapper로 기본 질의 12개를 한 ask session에서 실행했다. 이전 12회 `bootRun` 반복 방식과 달리 앱 시작은 한 번만 수행했고, chat model은 비활성화해 검색 context만 저장했다.

아티팩트: `build/rag-captures/20260622-retrieval-batch-12/summary.json`, `build/rag-captures/20260622-retrieval-batch-12/responses.jsonl`

검증: `python scripts\capture_sample_rag_retrieval.py --skip-build --skip-ingest --run-name 20260622-retrieval-batch-12` 실행 결과 `askSession.status=completed`, `returnCode=0`, `jsonObjectCount=12`, `durationSeconds=32.44`. 12개 record 모두 `answerMode=retrieval`, `model=none`, `contextCount=8`이고 replacement character count가 0임을 확인했다.

## 2026-06-22 11:59 - LLM 미사용 RAG retrieval 결과 보고용 실행

완료: 사용자 요청에 따라 LLM/chat 호출 없이 `scripts/capture_sample_rag_retrieval.py`로 기본 질의 12개를 실행했다. `SPRING_AI_MODEL_CHAT=none`과 `ChatClientAutoConfiguration` exclude가 적용된 retrieval-only run이다.

아티팩트: `build/rag-captures/20260622-retrieval-no-llm-report/summary.json`, `build/rag-captures/20260622-retrieval-no-llm-report/responses.jsonl`

검증: `python scripts\capture_sample_rag_retrieval.py --skip-build --skip-ingest --run-name 20260622-retrieval-no-llm-report` 실행 결과 `askSession.status=completed`, `returnCode=0`, `jsonObjectCount=12`, `durationSeconds=36.31`. 12개 record 모두 `answerMode=retrieval`, `model=none`, `contextCount=8`이고 replacement character count가 0임을 확인했다.

## 2026-06-22 12:05 - RAG retrieval chunk metadata report 추가

완료: capture script가 실행마다 `chunks.md`를 생성하도록 추가했다. `chunks.md`는 질의별로 반환된 모든 context chunk를 rank 순서로 펼치고, 각 chunk의 `title`, `noteId`, `chunkId`, `chunkIndex`, `score`, `textLength`, `text`를 함께 표시한다. `docs/technical/sample-notes-rag-cli.md`에도 `chunks.md`와 `chunkReportPath`를 문서화했다.

아티팩트: `scripts/capture_sample_rag_outputs.py`, `docs/technical/sample-notes-rag-cli.md`, `build/rag-captures/20260622-retrieval-chunk-report/chunks.md`

검증: `python -m py_compile scripts\capture_sample_rag_outputs.py scripts\capture_sample_rag_retrieval.py scripts\capture_sample_rag_chat.py` 통과. `python scripts\capture_sample_rag_retrieval.py --skip-build --skip-ingest --run-name 20260622-retrieval-chunk-report` 실행 결과 `jsonObjectCount=12`, `contexts_total=96`, `answerMode=retrieval`, `model=none`, replacement character count 0, `chunks.md` 생성 확인.

## 2026-06-22 10:40 - Note search index 상태 추가

완료: `NoteProjection`에 search index 동기화 상태와 마지막 성공 반영 version/hash를 추가했다. `NoteSearchIndexPort`의 replace/delete는 실제 적용 여부를 반환하고, Workspace note event handler는 성공 시 `INDEXED`/`PROVISIONAL`/`REMOVED`, 실패 시 `FAILED`, 미반영 target은 `STALE`로 저장한다. sample RAG ingest도 Qdrant 적용 성공 시 `INDEXED` projection을 남긴다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/events/note/NoteProjection.java`, `src/main/java/com/brainx/intelligence/infrastructure/events/note/NoteSearchIndexStatus.java`, `src/main/java/com/brainx/intelligence/infrastructure/events/note/WorkspaceNoteEventHandler.java`, `docs/technical/note-chunking.md`

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.events.note.WorkspaceNoteEventHandlerTest --tests com.brainx.intelligence.infrastructure.persistence.jpa.note.NoteProjectionJpaAdapterTest --tests com.brainx.intelligence.infrastructure.dev.rag.SampleRagServiceTest` 통과. `.\gradlew.bat --no-daemon check` 통과. `git diff --check` 통과. 운영 DB는 `ddl-auto=validate`이므로 `search_index_status`, `indexed_version`, `indexed_markdown_hash`, `indexed_at` 컬럼 추가 migration이 별도로 필요하다.

## 2026-06-22 11:10 - AI 모델 비용과 usage cost estimate 추가

완료: AI model catalog에 cached input 단가와 currency를 추가하고, `/api/v1/ai/models` 응답의 각 model item에 `vendorCachedInputCostPer1kTokens`, `costCurrency`, `enabled`를 추가했다. `TokenUsageRecord`는 cached/billable input, reasoning, total token, component별 estimated cost를 분리하도록 확장했다. `SpringAiAdapter`는 OpenAI native usage에서 cached/reasoning token을 추출하고, `SampleRagService.ask`는 LLM 응답 token usage를 `TokenUsagePort`로 기록하며 catalog 단가로 estimated vendor cost를 계산한다. `brainx.events.producer.enabled=true`이면 `KafkaIntelligenceEventAdapter`가 `TokenUsageRecordedRequested`와 `SemanticSearchPerformed` envelope를 Kafka topic으로 publish하고, 기본값은 NoOp fallback을 유지한다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `../../contracts-v2/brainx-asyncapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/resources/contracts/knowledge-intelligence.asyncapi.yaml`, `src/main/java/com/brainx/intelligence/settings/domain/VendorTokenCost.java`, `src/main/java/com/brainx/intelligence/shared/application/service/AiTokenUsageCostEstimator.java`, `src/main/java/com/brainx/intelligence/shared/application/port/outbound/TokenUsagePort.java`, `src/main/java/com/brainx/intelligence/infrastructure/events/producer/KafkaIntelligenceEventAdapter.java`, `src/main/java/com/brainx/intelligence/infrastructure/dev/rag/SampleRagService.java`, `docs/technical/ai-model-pricing-and-usage.md`

검증: `python scripts\extract_intelligence_openapi.py`, `python scripts\extract_intelligence_related_contracts.py` 성공. 관련 대상 테스트 `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.settings.domain.AiModelTest --tests com.brainx.intelligence.settings.domain.VendorTokenCostTest --tests com.brainx.intelligence.settings.application.usecase.AiSettingsServiceTest --tests com.brainx.intelligence.settings.adapter.web.SettingsControllerTest --tests com.brainx.intelligence.infrastructure.persistence.jpa.settings.SettingsJpaAdapterTest --tests com.brainx.intelligence.shared.application.service.AiTokenUsageCostEstimatorTest --tests com.brainx.intelligence.infrastructure.ai.SpringAiAdapterTest --tests com.brainx.intelligence.infrastructure.dev.rag.SampleRagServiceTest --tests com.brainx.intelligence.exploration.application.usecase.ExplorationServiceTest` 통과. Kafka adapter 대상 `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.events.producer.KafkaIntelligenceEventAdapterTest --tests com.brainx.intelligence.infrastructure.dev.rag.SampleRagServiceTest --tests com.brainx.intelligence.shared.application.service.AiTokenUsageCostEstimatorTest` 통과. 전체 `.\gradlew.bat --no-daemon check`, `.\gradlew.bat --no-daemon compileDevUiJava`, `git diff --check`, `git -C ..\..\contracts-v2 diff --check` 통과. 운영 DB는 `ddl-auto=validate`이므로 `ai_models.vendor_cached_input_cost_per_1k_tokens`, `ai_models.vendor_cost_currency` 컬럼 추가 migration과 catalog seed 정규화가 별도로 필요하다.

## 2026-06-22 11:36 - Voyage model local seed 추가

완료: 이미지로 전달된 Voyage embedding model 단가를 `local`/`dev-ui` profile용 `ai_models` seed로 추가했다. `voyage-4-large`, `voyage-4`, `voyage-4-lite`, `voyage-context-3`는 모두 provider `voyage`, currency `USD`, input 1,000 token당 단가 기준으로 저장한다. 기존 dev-ui OpenAI seed는 table count 기준 전체 skip을 제거해 Voyage seed와 실행 순서가 섞여도 OpenAI row가 누락되지 않게 했다. RAG CLI는 실제 provider 호출 모델을 runtime 설정으로 결정하고, DB catalog는 usage cost estimate와 model metadata 기준으로 사용하는 정책을 문서에 명시했다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/settings/LocalAiModelSeedData.java`, `src/test/java/com/brainx/intelligence/infrastructure/persistence/jpa/settings/LocalAiModelSeedDataTest.java`, `src/devUi/java/com/brainx/intelligence/devui/DevUiSeedData.java`, `docs/technical/ai-model-pricing-and-usage.md`, `docs/technical/sample-notes-rag-cli.md`

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.persistence.jpa.settings.LocalAiModelSeedDataTest`, `.\gradlew.bat --no-daemon compileDevUiJava`, `.\gradlew.bat --no-daemon check`, `git diff --check` 모두 통과. 운영 DB는 `ddl-auto=validate`이므로 이번 local/dev seed가 운영 catalog seed나 migration을 대체하지 않는다.

## 2026-06-22 12:15 - RAG vector 경로 Spring AI 제거

완료: RAG embedding/index/search 경로에서 Spring AI `VectorStore`와 `EmbeddingModel` 의존을 제거했다. Voyage embedding은 BrainX `AiEmbeddingPort` adapter가 직접 호출하고, Qdrant는 `io.qdrant:client` Java client wrapper로 직접 upsert/search/delete한다. document/query embedding은 각각 Voyage `input_type=document`/`query`를 사용하며, Voyage `usage.total_tokens`를 `note-search-index-embedding` 또는 `note-search-query-embedding` usage record로 기록하고 catalog 단가로 estimated cost를 계산한다. Spring AI는 chat adapter에만 남겼고, `ExplorationService`의 query text estimate는 entitlement/public response 용도로만 유지한다.

아티팩트: `build.gradle`, `src/main/java/com/brainx/intelligence/shared/application/port/outbound/AiEmbeddingPort.java`, `src/main/java/com/brainx/intelligence/infrastructure/ai/voyage/VoyageEmbeddingAdapter.java`, `src/main/java/com/brainx/intelligence/infrastructure/vector/qdrant`, `src/main/resources/application.yaml`, `docs/technical/vectorstore-embedding-model.md`, `docs/technical/ai-model-pricing-and-usage.md`

검증: `.\gradlew.bat --no-daemon compileJava compileTestJava` 통과. targeted tests `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.ai.voyage.* --tests com.brainx.intelligence.infrastructure.vector.qdrant.QdrantNoteSearchIndexAdapterTest --tests com.brainx.intelligence.exploration.application.usecase.ExplorationServiceTest --tests com.brainx.intelligence.infrastructure.dev.rag.SampleRagServiceTest --tests com.brainx.intelligence.infrastructure.events.note.WorkspaceNoteEventHandlerTest --tests com.brainx.intelligence.infrastructure.ai.SpringAiAdapterTest` 통과. 전체 `.\gradlew.bat --no-daemon check`, `.\gradlew.bat --no-daemon compileDevUiJava`, `git diff --check`, `git -C ..\..\contracts-v2 diff --check` 통과. 공개 REST/OpenAPI 계약 변경은 없다.

## 2026-06-22 12:36 - RAG context 기본 품질 필터 추가

완료: sample RAG CLI의 context 선택 단계에 `score >= 0.35` 기본 threshold와 같은 `noteId`당 최대 2개 chunk 제한을 추가했다. 필터 후 context가 없으면 LLM을 호출하지 않고 관련 chunk 없음으로 응답한다. sample note ingestion은 `sourcePath`, `sourceFilename` metadata를 Qdrant payload까지 전달하고, sample title 추출은 frontmatter `title`, H1, filename 순서로 정리했다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/dev/rag/SampleRagService.java`, `src/main/java/com/brainx/intelligence/infrastructure/dev/rag/SampleRagProperties.java`, `src/main/java/com/brainx/intelligence/infrastructure/dev/rag/SampleNoteLoader.java`, `src/main/java/com/brainx/intelligence/exploration/domain/NoteSearchDocument.java`, `src/main/java/com/brainx/intelligence/exploration/domain/NoteChunkSearchResult.java`, `src/main/java/com/brainx/intelligence/infrastructure/vector/qdrant/QdrantNoteSearchIndexAdapter.java`, `docs/technical/sample-notes-rag-cli.md`, `docs/technical/note-chunking.md`

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.dev.rag.SampleRagServiceTest --tests com.brainx.intelligence.infrastructure.dev.rag.SampleNoteLoaderTest --tests com.brainx.intelligence.infrastructure.vector.qdrant.QdrantNoteSearchIndexAdapterTest` 통과. 전체 `.\gradlew.bat --no-daemon check` 통과. `git diff --check`와 `rg "vaults/|AGENTS.md|knowledge-intelligence.openapi.yaml|knowledge-intelligence.asyncapi.yaml|knowledge-intelligence.consumed.openapi.yaml" README.md AGENTS.md vaults src/main/resources/contracts/README.md` 통과. 공개 REST/OpenAPI 계약 변경은 없다.

## 2026-06-22 14:20 - Domain TODO 진행 상태 체크

완료: `vaults/agents/domain-implementation-order.md`의 도메인 TODO에서 현재 구현 완료된 `AI 사용 준비`, `지식 탐색 기본 기능`을 완료로 체크했다. `docs/technical/consumed-events-implementation-checkpoints.md`에서도 이미 구현된 `NoteSearchIndexPort` note 단위 delete 선행 조건을 완료로 갱신했다. 작성 보조, public RAG chat, 연결 추천, 정리 제안, 클러스터링, 인사이트는 아직 후속 TODO로 남겨 두었다.

아티팩트: `vaults/agents/domain-implementation-order.md`, `docs/technical/consumed-events-implementation-checkpoints.md`

검증: 문서 전용 변경이므로 Gradle 검증은 생략했다. `git diff --check`와 `rg "vaults/|AGENTS.md|knowledge-intelligence.openapi.yaml|knowledge-intelligence.asyncapi.yaml|knowledge-intelligence.consumed.openapi.yaml" README.md AGENTS.md vaults src/main/resources/contracts/README.md`로 whitespace와 문서 라우팅을 확인했다.

## 2026-06-22 14:50 - 노트 작성 보조 API v1 구현

완료: `POST /api/v1/ai/inline-assists`를 SSE 응답 API로 구현했다. v1은 Workspace snapshot 없이 프론트가 보낸 `selectedText`, `contextBefore`, `contextAfter`만 prompt에 사용하고, `AiChatPort.generate(...)` 결과를 `delta` 1회와 `done` 이벤트로 반환한다. `SPELLCHECK`는 public enum에서 제거했고, `SUMMARIZE`, `REWRITE`, `CONTINUE`, `TRANSLATE` 입력 검증을 추가했다. `POST /api/v1/ai/suggestions/{suggestionId}/decision`은 Workspace patch 없이 `AiSuggestionDecisionRecorded` 결정 이벤트만 발행한다. inline assist chat usage는 `inline-assist-chat` featureId로 token usage/cost estimate를 기록한다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/assist`, `src/main/java/com/brainx/intelligence/infrastructure/events`, `src/main/resources/application.yaml`, `src/test/java/com/brainx/intelligence/assist`, `docs/technical/ai-model-pricing-and-usage.md`, `vaults/agents/domain-implementation-order.md`

검증: `python scripts\extract_intelligence_openapi.py`로 local OpenAPI slice 재생성 성공. targeted tests `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.assist.application.usecase.AssistServiceTest --tests com.brainx.intelligence.assist.adapter.web.AssistControllerTest --tests com.brainx.intelligence.infrastructure.events.producer.KafkaIntelligenceEventAdapterTest` 통과. 전체 `.\gradlew.bat --no-daemon check`, `.\gradlew.bat --no-daemon compileDevUiJava`, `git diff --check`, `git -C ..\..\contracts-v2 diff --check` 통과. `rg "SPELLCHECK" ../../contracts-v2/brainx-openapi.ssot.yaml src\main\resources\contracts\knowledge-intelligence.openapi.yaml`로 public 계약에서 제거를 확인했다.

## 2026-06-22 14:49 - RAG CLI token usage/cost capture 노출

완료: sample RAG CLI `ask` 응답에 LLM 호출 시 `tokenUsage`를 포함하도록 추가했다. `tokenUsage`는 input/cached/billable/output/reasoning/total token과 catalog 기반 `costEstimate`를 담으며, retrieval-only 응답은 LLM 호출이 없으므로 `tokenUsage=null`을 반환한다. capture script는 `summary.json`의 `runs[].tokenUsage`와 `chunks.md`의 질의별 header에 token/cost 요약을 표시한다. `responses.jsonl`에는 CLI full response가 그대로 저장된다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/dev/rag/SampleRagService.java`, `src/test/java/com/brainx/intelligence/infrastructure/dev/rag/SampleRagServiceTest.java`, `scripts/capture_sample_rag_outputs.py`, `docs/technical/sample-notes-rag-cli.md`, `build/rag-captures/20260622-chat-usage-smoke`, `build/rag-captures/20260622-chat-usage-with-ingest`, `build/rag-captures/20260622-chat-usage-qdrant-env`

검증: `python -m py_compile scripts\capture_sample_rag_outputs.py scripts\capture_sample_rag_retrieval.py scripts\capture_sample_rag_chat.py` 통과. `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.dev.rag.SampleRagServiceTest` 통과. 실제 capture run들은 파일 생성과 `tokenUsage=null` 저장을 확인했으나, 현재 로컬 검색 경로가 context 0개를 반환해 LLM 호출 및 실제 token/cost 값은 발생하지 않았다. Qdrant `brainx_note_search_voyage_1024` collection에는 211개 point가 있음을 확인했다.

## 2026-06-22 15:21 - RAG CLI Qdrant vector client bean 등록 수정

완료: sample RAG CLI retrieval이 context 0개를 반환하던 원인을 확인하고 수정했다. `QdrantClient`와 `voyageEmbeddingAdapter`는 등록됐지만 `DefaultQdrantVectorIndexClient`가 일반 component의 `@ConditionalOnBean(QdrantClient.class)` 조건 평가 순서 때문에 bean으로 등록되지 않아 `QdrantNoteSearchIndexAdapter.searchChunks()`가 즉시 빈 리스트를 반환했다. `DefaultQdrantVectorIndexClient`를 component scan 대상에서 제외하고 `QdrantVectorIndexConfiguration`의 명시적 `@Bean` method로 `QdrantVectorIndexClient`를 등록하도록 바꿨다. 로컬 `.brainx-local.properties`의 collection key도 현재 adapter가 읽는 `brainx.vector.qdrant.collection-name`으로 맞췄다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/vector/qdrant/DefaultQdrantVectorIndexClient.java`, `src/main/java/com/brainx/intelligence/infrastructure/vector/qdrant/QdrantVectorIndexConfiguration.java`, `src/test/java/com/brainx/intelligence/infrastructure/vector/qdrant/QdrantVectorIndexConfigurationTest.java`, `docs/technical/conditional-on-bean.md`, `build/rag-captures/20260622-retrieval-after-qdrant-client-fix`, `build/rag-captures/20260622-chat-after-qdrant-client-fix`

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.vector.qdrant.QdrantVectorIndexConfigurationTest --tests com.brainx.intelligence.infrastructure.vector.qdrant.QdrantNoteSearchIndexAdapterTest` 통과. `python scripts\capture_sample_rag_retrieval.py --skip-ingest --run-name 20260622-retrieval-after-qdrant-client-fix` 실행 결과 12개 질의 중 11개가 context를 반환했고 총 79개 context chunk가 저장됐다. `python scripts\capture_sample_rag_chat.py --skip-build --skip-ingest --run-name 20260622-chat-after-qdrant-client-fix "RAG 채팅 메시지 전송 API 경로와 응답 방식은?"` 실행 결과 `answerMode=llm`, `contextCount=5`, `inputTokens=2224`, `outputTokens=135`, `totalTokens=2359`가 저장됐다. chat cost estimate는 local `ai_models` catalog에 `gpt-5.4-mini` 단가 row가 없어 `unknown`이다.

## 2026-06-22 15:21 - 외부 자료 검색 port와 CLI v1 구현

완료: public REST API 없이 내부 재사용용 `ExternalSearchPort`를 추가하고, OpenAI Responses API `web_search` 기반 provider adapter와 NoOp fallback을 구현했다. OpenAI adapter는 `/v1/responses`를 직접 호출하며 `web_search`, `tool_choice=required`, `include=web_search_call.action.sources`를 사용하고, `output_text`, `url_citation`, search action source를 “답변 + 출처” 형태로 정규화한다. response usage가 있으면 `external-search-web` featureId로 token usage/cost estimate를 기록한다. 로컬 품질 확인용 `ExternalSearchApplicationRunner`는 단일 query와 stdin loop를 JSON 출력으로 지원한다.

아티팩트: `src/main/java/com/brainx/intelligence/shared/application/port/outbound/ExternalSearchPort.java`, `src/main/java/com/brainx/intelligence/infrastructure/search`, `src/main/java/com/brainx/intelligence/infrastructure/dev/search`, `src/main/resources/application.yaml`, `src/test/java/com/brainx/intelligence/infrastructure/search`, `src/test/java/com/brainx/intelligence/infrastructure/dev/search`, `docs/technical/external-search.md`, `docs/technical/README.md`, `docs/technical/ai-model-pricing-and-usage.md`, `vaults/INDEX.md`

검증: targeted tests `.\gradlew.bat --no-daemon test --tests "*ExternalSearch*" --console=plain` 통과. 전체 `.\gradlew.bat --no-daemon check --console=plain`, `.\gradlew.bat --no-daemon compileDevUiJava --console=plain`, `git diff --check` 통과. `rg "external-search|ExternalSearchPort|external-search-web" docs vaults src/main/java src/test/java src/main/resources/application.yaml`로 코드/문서 라우팅을 확인했다. 공개 REST/OpenAPI 계약 변경과 운영 DB migration은 없다.

## 2026-06-22 15:42 - RAG-only query embedding usage/cost capture

완료: sample RAG CLI retrieval-only 응답에서도 query embedding 사용량과 비용 추정을 확인할 수 있도록 `usageRecords[]`를 추가했다. `tokenUsage`는 기존처럼 LLM/chat convenience field로 유지하고, retrieval-only는 `tokenUsage=null`이지만 `usageRecords[].featureId=note-search-query-embedding`에 Voyage embedding model, input token, estimated cost를 담는다. sample RAG 실행 중 발생하는 `TokenUsagePort` record를 `SampleRagTokenUsageRecorder`가 ThreadLocal로 캡처하고 기존 Kafka/NoOp adapter로는 그대로 위임한다. capture script는 `summary.json`과 `chunks.md`에 `usageRecords` 요약을 표시한다. local profile의 `ai_models` seed가 sample RAG runner보다 늦게 실행되어 embedding cost가 `unknown`이 되던 문제는 `LocalAiModelSeedData`를 highest precedence runner로 고정해 수정했다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/dev/rag/SampleRagService.java`, `src/main/java/com/brainx/intelligence/infrastructure/dev/rag/SampleRagTokenUsageRecorder.java`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/settings/LocalAiModelSeedData.java`, `src/test/java/com/brainx/intelligence/infrastructure/dev/rag/SampleRagServiceTest.java`, `scripts/capture_sample_rag_outputs.py`, `docs/technical/sample-notes-rag-cli.md`, `docs/technical/ai-model-pricing-and-usage.md`, `build/rag-captures/20260622-retrieval-usage-cost-records`

검증: `python -m py_compile scripts\capture_sample_rag_outputs.py scripts\capture_sample_rag_retrieval.py scripts\capture_sample_rag_chat.py` 통과. `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.dev.rag.SampleRagServiceTest --tests com.brainx.intelligence.infrastructure.persistence.jpa.settings.LocalAiModelSeedDataTest --tests com.brainx.intelligence.infrastructure.vector.qdrant.QdrantNoteSearchIndexAdapterTest --tests com.brainx.intelligence.infrastructure.vector.qdrant.QdrantVectorIndexConfigurationTest` 통과. `python scripts\capture_sample_rag_retrieval.py --skip-ingest --run-name 20260622-retrieval-usage-cost-records "RAG 채팅 메시지 전송 API 경로와 응답 방식은?"` 실행 결과 `answerMode=retrieval`, `tokenUsage=null`, `usageRecords[0].model=voyage-4-lite`, `inputTokens=21`, `estimatedCost=4.2e-07 USD`, `contextCount=5`를 확인했다. `git diff --check -- <이번 작업 파일>` 통과.

## 2026-06-22 15:46 - local gpt-5.4-mini catalog seed 추가

완료: 이미지로 전달된 `gpt-5.4-mini` 단가표의 input `$0.75`, cached input `$0.075`, output `$4.50`을 local/dev catalog seed에 추가했다. provider 단가표는 1,000,000 token 기준으로 보고, 프로젝트 catalog 저장 기준인 1,000 token 기준으로 각각 `0.000750`, `0.000075`, `0.004500`으로 정규화했다. 이제 sample RAG chat mode에서 `brainx.dev.sample-rag.chat-model=gpt-5.4-mini`이면 catalog 기반 chat cost estimate를 계산할 수 있다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/settings/LocalAiModelSeedData.java`, `src/test/java/com/brainx/intelligence/infrastructure/persistence/jpa/settings/LocalAiModelSeedDataTest.java`, `docs/technical/ai-model-pricing-and-usage.md`, `docs/technical/sample-notes-rag-cli.md`

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.persistence.jpa.settings.LocalAiModelSeedDataTest` 통과. `git diff --check -- src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/settings/LocalAiModelSeedData.java src/test/java/com/brainx/intelligence/infrastructure/persistence/jpa/settings/LocalAiModelSeedDataTest.java docs/technical/ai-model-pricing-and-usage.md docs/technical/sample-notes-rag-cli.md` 통과. 실제 OpenAI 호출은 단가 seed 검증에 필요하지 않아 실행하지 않았다.

## 2026-06-22 15:49 - RAG 12개 retrieval-only 비용 포함 MD 보고서 생성

완료: 기존 12개 sample RAG 기본 질의를 retrieval-only 모드로 다시 실행하고, 질의별 query embedding 비용과 반환 chunk 본문이 포함된 Markdown 보고서를 생성했다. 보고서는 `chunks.md`에 질의별 `usageRecords` 요약과 각 context chunk의 metadata/text를 함께 기록한다.

아티팩트: `build/rag-captures/20260622-retrieval-12-md-cost-report/summary.json`, `build/rag-captures/20260622-retrieval-12-md-cost-report/responses.jsonl`, `build/rag-captures/20260622-retrieval-12-md-cost-report/chunks.md`

검증: `python scripts\capture_sample_rag_retrieval.py --skip-ingest --run-name 20260622-retrieval-12-md-cost-report` 실행 완료. 12개 질의 모두 `status=completed`, `answerMode=retrieval`로 저장됐고, 총 context chunk 79개, 총 query embedding 비용 `4.78e-06 USD`를 확인했다. `query-012`는 관련 chunk가 없어 `contextCount=0`이지만 query embedding usage/cost는 기록됐다.

## 2026-06-22 15:51 - RAG CLI Qdrant bean 누락 원인 분석 문서화

완료: Spring AI `VectorStore` 제거 이후 RAG CLI 검색이 안 된 원인을 `docs/technical/conditional-on-bean.md`에 사고 분석 형태로 정리했다. 결론은 클린 아키텍처나 Spring AI 제거 자체가 문제가 아니라, 새 `DefaultQdrantVectorIndexClient`를 일반 `@Component` + `@ConditionalOnBean(QdrantClient.class)`로 등록하면서 bean definition 처리 순서에 따라 `QdrantVectorIndexClient`만 누락된 것이다. `QdrantNoteSearchIndexAdapter`의 `ObjectProvider` no-op fallback이 이 누락을 예외가 아니라 빈 검색 결과로 숨겼다는 점과, dedicated `@Configuration`의 `@Bean` method/parameter dependency로 등록해야 한다는 재발 방지 기준을 함께 남겼다.

아티팩트: `docs/technical/conditional-on-bean.md`, `docs/technical/vectorstore-embedding-model.md`

검증: 문서 전용 변경이므로 Gradle 검증은 생략했다. `git diff --check -- docs/technical/conditional-on-bean.md docs/technical/vectorstore-embedding-model.md` 통과. `rg "RAG CLI 검색 실패 원인 분석|Spring AI VectorStore 제거|QdrantVectorIndexClient bean 미등록|재발 방지 기준|component scan 조건부 등록" docs/technical/conditional-on-bean.md docs/technical/vectorstore-embedding-model.md docs/technical/README.md vaults/INDEX.md`로 원인 분석 섹션과 기존 라우팅 문서를 확인했다.

## 2026-06-22 16:21 - RAG 12개 LLM 모드 비용 포함 MD 보고서 생성

완료: 기존 12개 sample RAG 기본 질의를 chat/LLM 모드로 실행하고, 질의별 GPT 답변 텍스트, GPT token/cost, query embedding usage/cost, context chunk metadata/text가 포함된 Markdown 보고서를 생성했다. `chunks.md`가 `response.answer`를 바로 펼치도록 capture script report writer도 보강했다. `query-012`는 context가 없어 LLM을 호출하지 않고 retrieval 응답으로 종료됐으며, query embedding 비용만 기록됐다.

아티팩트: `scripts/capture_sample_rag_outputs.py`, `docs/technical/sample-notes-rag-cli.md`, `build/rag-captures/20260622-chat-12-md-cost-report/summary.json`, `build/rag-captures/20260622-chat-12-md-cost-report/responses.jsonl`, `build/rag-captures/20260622-chat-12-md-cost-report/chunks.md`

검증: `python scripts\capture_sample_rag_chat.py --skip-ingest --run-name 20260622-chat-12-md-cost-report` 실행 완료. 12개 질의 모두 `status=completed`, 11개는 `answerMode=llm`, 1개는 context 없음으로 `answerMode=retrieval`이었다. 총 context chunk 79개, LLM input 36,450 token, LLM output 1,766 token, LLM total 38,216 token, LLM 비용 `0.0352845 USD`, embedding 비용 `4.78e-06 USD`, 총 추정 비용 `0.03528928 USD`를 확인했다. `python -m py_compile scripts\capture_sample_rag_outputs.py scripts\capture_sample_rag_retrieval.py scripts\capture_sample_rag_chat.py` 통과.

## 2026-06-22 17:03 - Document Group 기반 RAG/Search 격리 도입

완료: Intelligence-Service 안에서 `documentGroupId`를 semantic search, Workspace note projection, Qdrant vector payload/filter, sample RAG CLI의 격리 키로 추가했다. Workspace event payload나 snapshot에 group이 있으면 사용하고, 없거나 blank이면 `default`로 normalize한다. `NoteProjection` 저장/조회 key는 `userId + documentGroupId + noteId`가 됐고, Qdrant point id는 `userId::documentGroupId::noteId::chunkIndex` deterministic UUID를 사용한다. Public 계약에는 `SemanticSearchRequest.documentGroupId`, chat thread create/data의 `documentGroupId`, internal note snapshot의 optional `documentGroupId`를 반영했다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `../../contracts-v2/brainx-asyncapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/resources/contracts/knowledge-intelligence.consumed.openapi.yaml`, `src/main/resources/contracts/knowledge-intelligence.asyncapi.yaml`, `src/main/java/com/brainx/intelligence/shared/domain/DocumentGroups.java`, `src/main/java/com/brainx/intelligence/exploration`, `src/main/java/com/brainx/intelligence/infrastructure/events/note`, `src/main/java/com/brainx/intelligence/infrastructure/vector/qdrant`, `src/main/java/com/brainx/intelligence/infrastructure/dev/rag`, `docs/technical/note-chunking.md`, `docs/technical/vectorstore-embedding-model.md`, `docs/technical/consumed-events-implementation-checkpoints.md`, `docs/technical/sample-notes-rag-cli.md`

주의: 운영 DB는 `ddl-auto=validate`이므로 `intelligence_note_projections.document_group_id` 컬럼 추가, 기존 row의 `default` backfill, 가능하면 `projection_id`를 `user_id::default::note_id` 형태로 갱신하는 migration이 별도로 필요하다. 기존 Qdrant point에는 `documentGroupId` payload가 없을 수 있어 group filter 적용 후 검색에서 누락될 수 있으므로 재ingest를 권장한다.

검증: `python scripts\extract_intelligence_openapi.py`, `python scripts\extract_intelligence_related_contracts.py`, `python -m py_compile scripts\capture_sample_rag_outputs.py scripts\capture_sample_rag_retrieval.py scripts\capture_sample_rag_chat.py` 통과. targeted tests `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.events.note.WorkspaceNoteEventHandlerTest --tests com.brainx.intelligence.infrastructure.persistence.jpa.note.NoteProjectionJpaAdapterTest --tests com.brainx.intelligence.infrastructure.vector.qdrant.QdrantNoteSearchIndexAdapterTest --tests com.brainx.intelligence.exploration.application.usecase.ExplorationServiceTest --tests com.brainx.intelligence.exploration.adapter.web.ExplorationControllerTest --tests com.brainx.intelligence.infrastructure.dev.rag.SampleRagServiceTest --tests com.brainx.intelligence.infrastructure.events.producer.KafkaIntelligenceEventAdapterTest` 통과. 전체 `.\gradlew.bat --no-daemon check`, `.\gradlew.bat --no-daemon compileDevUiJava`, `git diff --check`, `git -C ..\..\contracts-v2 diff --check` 통과.

## 2026-06-23 11:56 - RAG 채팅 public API 구현

완료: 기존 OpenAPI 계약을 변경하지 않고 `/api/v1/ai/chat-threads` 생성, `/api/v1/ai/chat-threads/{threadId}/messages` SSE 전송, `/api/v1/ai/chat-threads/{threadId}` 조회를 구현했다. v1 RAG는 `NoteChunkRetrievalPort` 기반 노트 chunk context만 사용하며, `brainx.chat.rag.*` 설정으로 topK, minScore, note당 chunk 수, context 길이를 조정한다. 메시지 전송은 user message 저장, `RAG_CHAT` entitlement 확인, 실시간 `delta` SSE, 완료 후 assistant message 저장, `ChatMessageCreated`와 `TokenUsageRecordedRequested` 발행 순서로 동작한다. stream provider usage를 현재 포트에서 받을 수 없으므로 prompt/answer 문자 길이 기반 추정 token usage를 기록한다.

아티팩트: `src/main/java/com/brainx/intelligence/chat`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/chat`, `src/main/java/com/brainx/intelligence/infrastructure/events/producer/KafkaIntelligenceEventAdapter.java`, `src/main/resources/application.yaml`, `src/test/java/com/brainx/intelligence/chat`, `src/test/java/com/brainx/intelligence/infrastructure/persistence/jpa/chat`

주의: 운영 DB는 `ddl-auto=validate`이므로 `intelligence_chat_threads`, `intelligence_chat_messages` 테이블 생성 migration이 별도로 필요하다. 이 repo에는 Flyway/Liquibase migration이 없어 local/test H2 `create-drop`만 자동 생성한다.

검증: targeted tests `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.chat.application.usecase.ChatServiceTest --tests com.brainx.intelligence.chat.adapter.web.ChatControllerTest --tests com.brainx.intelligence.infrastructure.persistence.jpa.chat.ChatJpaAdapterTest --tests com.brainx.intelligence.infrastructure.events.producer.KafkaIntelligenceEventAdapterTest --console=plain` 통과. 전체 `.\gradlew.bat --no-daemon check --console=plain`, `.\gradlew.bat --no-daemon compileDevUiJava --console=plain`, `git diff --check` 통과.

## 2026-06-23 14:15 - Frontend 연동 guide 추가

완료: 상위 repository의 `../../brainx-next`가 BrainX 프론트엔드 프로젝트이며, Intelligence Service 작업은 해당 프론트가 필요로 하는 Knowledge Intelligence API를 OpenAPI 계약 기준으로 구현한다는 기준을 문서화했다. `/api/v1` proxy 기준, 프론트 호출 위치 확인 순서, mock API와 public 계약의 우선순위, SSE/다른 도메인 source-of-truth 확인 기준을 `vaults/agents/frontend-integration.md`에 정리하고 `vaults/INDEX.md`에 라우팅을 추가했다.

아티팩트: `vaults/agents/frontend-integration.md`, `vaults/INDEX.md`

검증: 문서 전용 변경이므로 Gradle 검증은 생략했다. `rg "vaults/|AGENTS.md|knowledge-intelligence.openapi.yaml|knowledge-intelligence.asyncapi.yaml|knowledge-intelligence.consumed.openapi.yaml|frontend-integration|brainx-next" README.md AGENTS.md vaults src/main/resources/contracts/README.md`로 새 guide 라우팅과 핵심 path를 확인했다.

## 2026-06-23 15:32 - Inline rewrite Markdown 보존

완료: `brainx-next` 노트 편집기의 AI 다시쓰기 수락 흐름에서 선택 영역을 plain text로만 복사/삽입하던 문제를 수정했다. 선택된 ProseMirror slice를 Markdown-compatible 문자열로 직렬화해 `selectedText`, `contextBefore`, `contextAfter`에 전달하고, AI 응답은 Markdown/HTML 삽입 경로로 파싱해 bold/link/code/list/heading 등 기본 Markdown 서식을 보존하도록 했다. 수락 직전에는 원래 선택 plain text가 그대로 남아 있는지 확인해 사용자가 본문을 바꾼 뒤 잘못 덮어쓰는 것을 막는다. 백엔드 `AssistService` system prompt에는 Markdown 구조를 의미 있게 유지하라는 지시를 추가했다.

아티팩트: `../../brainx-next/components/notes/NoteEditor.tsx`, `src/main/java/com/brainx/intelligence/assist/application/usecase/AssistService.java`

검증: `npm run typecheck` 통과. `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.assist.application.usecase.AssistServiceTest --tests com.brainx.intelligence.assist.adapter.web.AssistControllerTest --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. `git diff --check -- src/main/java/com/brainx/intelligence/assist/application/usecase/AssistService.java vaults/worklogs/2026-06.md ../../brainx-next/components/notes/NoteEditor.tsx` 통과.

## 2026-06-23 14:55 - Intelligence Service 기본 포트 8086 변경

완료: Intelligence-Service 단독 실행 시 기본 `server.port`가 `8086`이 되도록 `SERVER_PORT` fallback 값을 변경했다. 명시적인 `SERVER_PORT` 환경변수는 기존처럼 기본값보다 우선한다.

아티팩트: `src/main/resources/application.yaml`, `src/main/resources/application.yml`

검증: `.\gradlew.bat --no-daemon compileJava --console=plain`, `git diff --check` 통과.

## 2026-06-24 15:28 - 로컬 실행 문서 8086/local secret 기준 갱신

완료: README 실행 섹션을 `local` profile, 8086 port, `.brainx-local.properties` optional import 기준으로 갱신했다. `.brainx-local.properties`는 git ignore 대상이며 `local` profile 없이 실행하면 읽히지 않는다는 점을 명시했다.

아티팩트: `README.md`

검증: 문서 전용 변경이므로 Gradle 검증은 생략했다. `rg "brainx-local|spring.profiles.active=local|8086|Swagger UI|actuator/health" README.md src/main/resources/application-local.yaml`와 `git diff --check -- README.md vaults/worklogs/2026-06.md` 통과.

## 2026-06-23 15:46 - Inline rewrite context prompt 제한

완료: AI 다시쓰기 prompt에서 `Context Before`와 `Context After`는 immutable reference context이고, `REWRITE`/`TRANSLATE` 출력은 `Selected` replacement만 포함해야 한다는 규칙을 명시했다. user prompt 라벨도 reference-only context와 replace 대상 section을 구분하도록 바꿨고, assist use case test가 해당 prompt contract를 확인하도록 갱신했다.

아티팩트: `src/main/java/com/brainx/intelligence/assist/application/usecase/AssistService.java`, `src/test/java/com/brainx/intelligence/assist/application/usecase/AssistServiceTest.java`

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.assist.application.usecase.AssistServiceTest --tests com.brainx.intelligence.assist.adapter.web.AssistControllerTest --console=plain` 통과. `.\gradlew.bat --no-daemon clean check --console=plain`은 실행 중인 dev-server 로그 파일이 열려 있어 `clean` 단계에서 실패했고, 이어서 실행한 `.\gradlew.bat --no-daemon check --console=plain`은 통과했다. `git diff --check -- src/main/java/com/brainx/intelligence/assist/application/usecase/AssistService.java src/test/java/com/brainx/intelligence/assist/application/usecase/AssistServiceTest.java vaults/worklogs/2026-06.md` 통과.

## 2026-06-23 16:35 - Inline assist frontend stream lifecycle 원인 문서화

완료: `brainx-next`의 `AI로 요약` 응답이 채팅에 표시되지 않던 원인을 `docs/technical/inline-assist-frontend-stream-lifecycle.md`에 정리했다. 원인은 Intelligence Service endpoint나 Next proxy가 아니라, `RightSidebar`가 `onAiRequestHandled()`로 `pendingAiRequest`를 비우면서 같은 `useEffect` cleanup에서 진행 중인 SSE `AbortController`를 즉시 abort하던 lifecycle coupling이었다. 문서에는 증상, 직접 SSE 호출로 원인을 분리한 과정, 수정 기준, 재발 방지 기준을 남겼고 `vaults/INDEX.md`에 라우팅을 추가했다.

아티팩트: `docs/technical/inline-assist-frontend-stream-lifecycle.md`, `vaults/INDEX.md`

검증: 문서 전용 변경이므로 Gradle 검증은 생략했다. `rg "inline-assist-frontend-stream-lifecycle|pendingAiRequest|AbortController|사용자 메시지만 표시|SSE" docs/technical/inline-assist-frontend-stream-lifecycle.md vaults/INDEX.md`로 문서 본문과 라우팅을 확인했다. `git diff --check -- docs/technical/inline-assist-frontend-stream-lifecycle.md vaults/INDEX.md vaults/worklogs/2026-06.md` 통과.

## 2026-06-24 15:00 - Frontend 주도 AI context 계약 및 연결

완료: Chat message 계약에 `clientContext`를 추가하고, 백엔드 chat prompt가 프론트가 보낸 typed context를 우선 사용하도록 했다. `clientContext`가 있으면 RAG chunk retrieval을 건너뛰고, `mode/source/items`와 각 item의 `type`, `noteId`, `documentGroupId`, `truncated`, `text`를 prompt에 포함한다. 기존 `noteScope`는 thread/documentGroup 검증용으로 유지했다. 프론트 `brainx-next`에는 `lib/ai-context/` 모듈을 추가해 노트 HTML/Markdown 정규화, context budget, 작업별 context builder를 분리했고, `RightSidebar`의 일반 질문/선택 요약은 chat SSE에 `clientContext`를 포함해 전송하도록 바꿨다. `NoteEditor` 다시쓰기/이어쓰기 inline assist payload도 공통 builder를 통과하게 했다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/chat`, `src/test/java/com/brainx/intelligence/chat`, `src/test/java/com/brainx/intelligence/infrastructure/persistence/jpa/chat/ChatJpaAdapterTest.java`, `../../brainx-next/lib/ai-context`, `../../brainx-next/lib/generated/intelligence-openapi.ts`, `../../brainx-next/components/notes/RightSidebar.tsx`, `../../brainx-next/components/notes/NoteEditor.tsx`

주의: 운영 DB는 `ddl-auto=validate`이므로 기존 `intelligence_chat_messages` 테이블에 `client_context` 컬럼 추가 및 기존 row의 `{}` backfill migration이 별도로 필요하다. 프론트에는 별도 unit test runner가 없어 context builder 검증은 `npm run typecheck`와 백엔드 contract/binding 테스트로 확인했다.

검증: `python scripts\extract_intelligence_openapi.py`, `npm run generate:intelligence-api-types`, `npm run typecheck` 통과. targeted tests `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.chat.application.usecase.ChatServiceTest --tests com.brainx.intelligence.chat.adapter.web.ChatControllerTest --tests com.brainx.intelligence.infrastructure.persistence.jpa.chat.ChatJpaAdapterTest --console=plain` 통과. 전체 `.\gradlew.bat --no-daemon test --console=plain`, `git diff --check` 통과.

## 2026-06-24 15:15 - Frontend AI context 관리 문서화

완료: `brainx-next`의 프론트 주도 AI context 관리 구현을 `docs/technical/frontend-ai-context-management.md`에 문서화했다. 문서에는 `clientContext`와 `noteScope`의 역할 분리, `lib/ai-context` 모듈 책임, 작업별 context budget, RightSidebar chat flow, NoteEditor inline assist flow, backend prompt 우선순위, 운영 주의사항, stale backend troubleshooting을 정리했다. 새 문서가 `docs/technical/README.md`와 `vaults/INDEX.md`에서 라우팅되도록 추가했다.

아티팩트: `docs/technical/frontend-ai-context-management.md`, `docs/technical/README.md`, `vaults/INDEX.md`

검증: 문서 전용 변경이므로 Gradle 검증은 생략했다. `rg "frontend-ai-context-management|clientContext|noteScope|RightSidebar|buildNoteAiContext|buildInlineAssistContext|stale backend" docs/technical/frontend-ai-context-management.md docs/technical/README.md vaults/INDEX.md`로 본문과 라우팅을 확인했다. `git diff --check -- docs/technical/frontend-ai-context-management.md docs/technical/README.md vaults/INDEX.md` 통과.

## 2026-06-24 15:35 - Semantic search USER/DOCUMENT_GROUP scope 분리

완료: public `semantic-search` 요청에 optional `scope`를 추가하고 `DOCUMENT_GROUP` 기본 동작과 `USER` 전체 노트 검색 경로를 분리했다. `USER` scope에서는 `documentGroupId`를 금지하고, Qdrant 검색 filter는 `userId`만 적용한다. `DOCUMENT_GROUP` scope와 RAG chat chunk retrieval은 기존처럼 `userId + documentGroupId` 경계로 유지했다. `SemanticSearchPerformed` 이벤트에는 `scope`를 포함하고, `USER` scope event payload의 `documentGroupId`는 null로 발행한다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `../../contracts-v2/brainx-asyncapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/resources/contracts/knowledge-intelligence.asyncapi.yaml`, `src/main/java/com/brainx/intelligence/exploration/domain/SearchScope.java`, `src/main/java/com/brainx/intelligence/exploration`, `src/main/java/com/brainx/intelligence/infrastructure/vector/qdrant`, `src/main/java/com/brainx/intelligence/infrastructure/events/producer/KafkaIntelligenceEventAdapter.java`, `../../brainx-next/package.json`, `../../brainx-next/lib/generated/intelligence-openapi.ts`

검증: `python scripts\extract_intelligence_openapi.py`, `python scripts\extract_intelligence_related_contracts.py`, `npm run generate:intelligence-api-types`, `npm run typecheck` 통과. targeted tests `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.exploration.domain.ExplorationDomainTest --tests com.brainx.intelligence.exploration.application.usecase.ExplorationServiceTest --tests com.brainx.intelligence.exploration.adapter.web.ExplorationControllerTest --tests com.brainx.intelligence.infrastructure.vector.qdrant.QdrantNoteSearchIndexAdapterTest --tests com.brainx.intelligence.infrastructure.events.producer.KafkaIntelligenceEventAdapterTest --console=plain` 통과. 전체 `.\gradlew.bat --no-daemon test --console=plain`, `git diff --check` 통과.

## 2026-06-24 16:02 - RightSidebar note-scoped chat guard 추가

완료: `clientContext.source=RIGHT_SIDEBAR`인 chat request에 한해 system prompt에 note-scoped guard를 추가했다. 사이드바 채팅은 먼저 질문이 현재 노트, 선택 영역, 노트 문맥 작업과 관련 있는지 판단하고, 관련 없는 외부 질문이면 답변하지 않고 현재 노트 관련 질문만 처리한다는 안내를 반환하도록 지시한다. `WORKSPACE_CHAT` 등 다른 surface에는 이 guard를 적용하지 않는다.

아티팩트: `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatService.java`, `src/test/java/com/brainx/intelligence/chat/application/usecase/ChatServiceTest.java`

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.chat.application.usecase.ChatServiceTest --console=plain` 통과.

## 2026-06-25 11:15 - 유사 노트 자동 연결 v1 내부 CLI 구현

완료: 공개 REST/OpenAPI 계약 변경 없이 내부 `NoteAutoLinkUseCase`와 dev CLI를 추가했다. `VECTOR_LLM`은 기존 Qdrant chunk 검색을 1차 후보 필터로 사용하고, `LLM_ONLY`는 group note card만으로 LLM 후보를 생성한다. 두 전략 모두 raw markdown에서 anchor 위치를 검증하며, 기존 markdown link/wiki link/code 영역 안의 anchor는 제외한다. `NoteProjection` read model에는 raw `markdown`을 nullable CLOB로 추가했고 snapshot/sample ingest 성공 시 저장한다.

아티팩트: `src/main/java/com/brainx/intelligence/autolink`, `src/main/java/com/brainx/intelligence/infrastructure/dev/autolink`, `src/main/java/com/brainx/intelligence/infrastructure/events/note/NoteProjection.java`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/note`, `src/main/resources/application.yaml`, `docs/technical/note-auto-linking.md`, `docs/technical/ai-model-pricing-and-usage.md`, `docs/technical/note-chunking.md`, `docs/technical/README.md`, `vaults/INDEX.md`

주의: 운영 DB는 `ddl-auto=validate`이므로 `intelligence_note_projections.markdown` CLOB/TEXT 컬럼 추가 migration이 별도로 필요하다.

검증: targeted autolink tests `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.autolink.* --tests com.brainx.intelligence.infrastructure.dev.autolink.NoteAutoLinkApplicationRunnerTest --console=plain` 통과. projection/event/sample tests `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.persistence.jpa.note.NoteProjectionJpaAdapterTest --tests com.brainx.intelligence.infrastructure.events.note.WorkspaceNoteEventHandlerTest --tests com.brainx.intelligence.infrastructure.dev.rag.SampleRagServiceTest --console=plain` 통과. 전체 `.\gradlew.bat --no-daemon check --console=plain`, `.\gradlew.bat --no-daemon compileDevUiJava --console=plain`, `git diff --check` 통과.

## 2026-06-24 16:50 - 본 채팅 LLM 라우터와 CLI 추가

완료: 본 채팅 전송 흐름에 LLM 기반 1차 route classifier를 추가했다. 라우터는 `NOTE_QA`, `WORKSPACE_SEARCH`, `COMPOSE`, `NOTE_ACTION`, `OUT_OF_SCOPE` 중 하나를 strict JSON으로 반환하고, invalid/provider failure는 `OUT_OF_SCOPE`로 fail closed 처리한다. SSE stream 첫 이벤트로 `route`를 내보내며, `WORKSPACE_SEARCH`는 user-wide chunk retrieval, `NOTE_QA`는 thread document group retrieval을 사용한다. `COMPOSE`와 `NOTE_ACTION`은 context가 없으면 retrieval 없이 답변 생성을 진행하고, `NOTE_ACTION`은 저장/삽입 mutation 없이 Markdown 초안만 생성한다. 개발용 `brainx.dev.chat-router` CLI도 추가해 sample notes ingest 후 실제 `ChatService` 흐름의 route, answer, citation, event JSON을 확인할 수 있게 했다.

아티팩트: `src/main/java/com/brainx/intelligence/chat/domain/ChatRoute.java`, `src/main/java/com/brainx/intelligence/chat/domain/ChatRouteDecision.java`, `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatRouteDecider.java`, `src/main/java/com/brainx/intelligence/chat/application/usecase/LlmChatRouteDecider.java`, `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatRouterProperties.java`, `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatService.java`, `src/main/java/com/brainx/intelligence/infrastructure/dev/chatrouter`, `src/main/java/com/brainx/intelligence/exploration/application/port/outbound/NoteChunkRetrievalPort.java`, `src/main/java/com/brainx/intelligence/infrastructure/vector/qdrant/QdrantNoteSearchIndexAdapter.java`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/settings/LocalAiModelSeedData.java`, `src/main/resources/application.yaml`, `docs/technical/ai-model-pricing-and-usage.md`, `docs/technical/sample-notes-rag-cli.md`

검증: targeted tests `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.chat.application.usecase.ChatRouteDeciderTest --tests com.brainx.intelligence.chat.application.usecase.ChatServiceTest --tests com.brainx.intelligence.chat.adapter.web.ChatControllerTest --tests com.brainx.intelligence.infrastructure.vector.qdrant.QdrantNoteSearchIndexAdapterTest --tests com.brainx.intelligence.infrastructure.persistence.jpa.settings.LocalAiModelSeedDataTest --tests com.brainx.intelligence.infrastructure.dev.chatrouter.ChatRouterApplicationRunnerTest --console=plain` 통과. 전체 `.\gradlew.bat --no-daemon check --console=plain`, `git diff --check` 통과.

## 2026-06-25 09:37 - Chat Router CLI capture script 추가

완료: `scripts/capture_chat_router_cli.py`를 추가해 본 채팅 라우터 CLI의 실제 local integration smoke test를 capture할 수 있게 했다. script는 `.brainx-local.properties`, OpenAI/Voyage/Qdrant 설정, Qdrant gRPC 연결을 preflight로 확인하고, 실패 시 provider 호출 없이 `summary.json`과 `report.md`에 실패 이유를 남긴다. preflight 통과 시 `ingest-and-ask`로 sample notes를 색인한 뒤 기본 5개 route 시나리오(`NOTE_QA`, `WORKSPACE_SEARCH`, `COMPOSE`, `NOTE_ACTION`, `OUT_OF_SCOPE`)를 실행하고 expected/actual route, answer preview, citation count, SSE event를 저장한다.

아티팩트: `scripts/capture_chat_router_cli.py`, `docs/technical/sample-notes-rag-cli.md`, `vaults/worklogs/2026-06.md`

검증: `python -m py_compile scripts\capture_chat_router_cli.py` 통과. `python scripts\capture_chat_router_cli.py --run-name 20260625-chat-router-alias-check`는 `.brainx-local.properties`의 Spring property key alias를 인식해 OpenAI/Voyage/Qdrant collection 설정은 통과했고, Qdrant gRPC `localhost:6334` 연결 불가로 preflight 단계에서 exit code `2`로 종료되는 것을 확인했다. `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.dev.chatrouter.ChatRouterApplicationRunnerTest --console=plain`, `git diff --check` 통과.

## 2026-06-25 10:33 - Chat Router NOTE_QA 우선순위 보강

완료: 라우터가 `현재 문서 그룹 노트 기준으로 ... 설명해줘` 요청을 `WORKSPACE_SEARCH`로 분류하던 문제를 수정했다. `LlmChatRouteDecider` system prompt에 `현재 노트`, `이 노트`, `선택 텍스트`, `현재 문서 그룹`, `현재 thread` 같은 현재 scope 표현은 저장/삽입 요청이 아닌 한 `NOTE_QA`를 우선 선택하고, `내 전체 노트`, `워크스페이스 전체`, `모든 노트` 같은 user-wide 표현만 `WORKSPACE_SEARCH`로 보낸다는 우선순위와 예시를 추가했다.

아티팩트: `src/main/java/com/brainx/intelligence/chat/application/usecase/LlmChatRouteDecider.java`, `src/test/java/com/brainx/intelligence/chat/application/usecase/ChatRouteDeciderTest.java`, `vaults/worklogs/2026-06.md`

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.chat.application.usecase.ChatRouteDeciderTest --console=plain` 통과. `python scripts\capture_chat_router_cli.py --run-name 20260625-chat-router-prompt-fix` 실행 결과 preflight, bootJar, ingest-and-ask, ask session이 모두 성공했고 5개 시나리오가 모두 기대 route로 통과했다. `git diff --check` 통과.

## 2026-06-25 15:19 - AI context 부족 guard 추가

완료: 사이드바 AI와 인라인 AI가 너무 작은 context로도 응답을 생성하지 않도록 deterministic guard를 추가했다. `ChatService`는 `RIGHT_SIDEBAR` clientContext가 있는 `NOTE_QA`/`WORKSPACE_SEARCH`에서 본문/선택 영역이 80자 미만이면 provider 호출과 retrieval 없이 고정 안내 답변을 저장/스트리밍한다. `COMPOSE`와 `NOTE_ACTION`은 계획대로 guard 대상에서 제외했다. `AssistService`는 `SUMMARIZE` 합산 80자, `CONTINUE` 앞 문맥 120자, `REWRITE`/`TRANSLATE` 선택 영역 10자 기준을 적용하고 부족하면 suggestion/usage/event 생성 전에 `IllegalArgumentException`으로 중단한다. `brainx-next`에는 `validateAiContextSufficiency()`를 추가해 `RightSidebar` 질문/요약과 `NoteEditor` 다시쓰기/이어쓰기에서 같은 기준으로 선제 차단한다.

아티팩트: `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatService.java`, `src/main/java/com/brainx/intelligence/assist/application/usecase/AssistService.java`, `src/test/java/com/brainx/intelligence/chat/application/usecase/ChatServiceTest.java`, `src/test/java/com/brainx/intelligence/assist/application/usecase/AssistServiceTest.java`, `../../brainx-next/lib/ai-context`, `../../brainx-next/components/notes/RightSidebar.tsx`, `../../brainx-next/components/notes/NoteEditor.tsx`, `../../brainx-next/lib/use-guide-store.ts`

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.chat.application.usecase.ChatServiceTest --tests com.brainx.intelligence.assist.application.usecase.AssistServiceTest --console=plain` 통과. `npm run typecheck` 통과. `git diff --check` 통과.

## 2026-06-25 16:05 - Consumed event 계약 일치 수준 문서화

완료: 구현된 consumed event 7개(`NoteCreated`, `NoteContentSaved`, `NoteMetadataChanged`, `NoteTagsChanged`, `NoteTrashed`, `NoteDeleted`, `NotesMoved`)와 AsyncAPI SSOT의 topic/eventType/payload required field 일치 수준을 `docs/technical/consumed-event-contract-alignment.md`에 정리했다. SSOT에는 있지만 아직 handler와 기본 topic 구독에 없는 `NoteLinkCreated`, `NoteLinkDeleted`, folder 이벤트, `CaptureReceived`, `UserDeletionRequested`도 별도 표로 남겼고, 다음 consumer 구현 시 문서 갱신 규칙을 추가했다.

아티팩트: `docs/technical/consumed-event-contract-alignment.md`, `docs/technical/README.md`, `vaults/INDEX.md`, `vaults/worklogs/2026-06.md`

검증: 문서 전용 변경이므로 Gradle 검증은 생략했다. `rg "consumed-event-contract-alignment|NoteContentSaved|savedAt|NoteTrashed|UserDeletionRequested|Alignment Levels" docs/technical/consumed-event-contract-alignment.md docs/technical/README.md vaults/INDEX.md`와 `git diff --check -- docs/technical/consumed-event-contract-alignment.md docs/technical/README.md vaults/INDEX.md vaults/worklogs/2026-06.md`로 문서 본문과 라우팅을 확인했다.
2026-06-25 16:00 - Autolink 품질 필터 개선

완료: 실제 `sample_notes` 평가에서 확인된 일반 anchor, source-target 중복, target flooding 문제를 줄이기 위해 `NoteAutoLinkService` 후처리 품질 필터와 ranking을 추가했다. 기본 threshold는 `min-confidence=0.75`, `vector-strong-score=0.60`, 한글 단일 anchor 최소 4글자, source-target당 1개, target note당 5개로 둔다. `filteredInvalidAnchorCount`와 별도로 `filteredQualityCount`를 CLI JSON에 노출하도록 내부 결과 DTO를 확장했다.

검증: targeted autolink tests `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.autolink.* --tests com.brainx.intelligence.infrastructure.dev.autolink.NoteAutoLinkApplicationRunnerTest --console=plain` 통과. 전체 `.\gradlew.bat --no-daemon check --console=plain`, `git diff --check` 통과. 실제 평가용 ingest는 `build/autolink-eval/ingest-tuned.log` 기준 `notesIndexed=21`, `chunksIndexed=211`로 성공했다. `COMPARE` 재평가는 OpenAI chat 호출이 `HTTP 429 insufficient_quota`를 반환해 완료하지 못했다.

2026-06-25 16:29 - Autolink 튜닝 후 실제 재평가

완료: OpenAI API credit 복구 후 같은 `sample_notes` 21개, H2 file DB `intelligence_service_tuned`, Qdrant collection `brainx_note_autolink_eval_tuned_20260625`로 `COMPARE` 평가를 재실행했다. 결과는 `build/autolink-eval/autolink-result-tuned.json`, `autolink-summary-tuned.json`, `autolink-suggestions-tuned.md`, `evaluation-report-tuned.md`에 남겼다.

결과: `VECTOR_LLM`은 73개에서 36개로 줄고 `filteredQualityCount=43`, `filteredInvalidAnchorCount=18`이 됐다. `LLM_ONLY`는 67개에서 43개로 줄고 `filteredQualityCount=40`, `filteredInvalidAnchorCount=16`이 됐다. 일반 단일 anchor(`Kafka`, `이벤트`, `MSA`, `랜딩`, `목록`)는 tuned suggestions에서 사라졌다. 남은 개선점은 같은 제목/근접 중복 note 억제, 구체 anchor지만 관계가 넓은 weak link 재분류, cached-input 비용 조건 재평가다.

검증: targeted autolink tests 재실행 통과. `COMPARE` 실제 실행은 exit code 0으로 완료했다.
2026-06-25 17:02 - Autolink 중복 제목과 weak relation 필터 개선

완료: tuned 평가에서 남은 중복/weak relation 문제를 줄이기 위해 `NoteAutoLinkService`에 duplicate-title filter, broad weak relation rule, optional relation verifier를 추가했다. source/target title은 괄호 부제, 구두점, 공백, 대소문자를 무시해 비교하고, 같은 제목/near-duplicate title 추천은 `filteredDuplicateTitleCount`로 집계한다. broad architecture/API/contract target과 source 문맥의 lexical overlap이 낮거나 file/markdown 저장 작업성 source title에서 broad target으로 향하는 추천은 `filteredWeakRelationCount`로 제외한다. 애매한 후보는 `note-auto-link-relation-verifier-chat` usage로 LLM relation verifier를 호출한다.

결과: 기존 tuned H2/Qdrant 평가 데이터를 재사용해 `COMPARE`를 재실행했고 산출물은 `build/autolink-eval/autolink-result-relation.json`, `autolink-summary-relation.json`, `autolink-suggestions-relation.md`, `evaluation-report-relation.md`에 남겼다. relation 개선 후 `VECTOR_LLM`은 34개 제안, `filteredWeakRelationCount=4`, 비용 `$0.0662794`; `LLM_ONLY`는 37개 제안, `filteredWeakRelationCount=9`, 비용 `$0.07013145`가 됐다. 이전에 남았던 `BrainX - 뇌 탐사 -> BrainX - 뇌 탐사`, `Save the markdown content to a file -> BrainX 도메인 기준 MSA / API / 이벤트 계약`, 일반 단일 anchor 후보는 최종 suggestions에서 사라졌다.

검증: targeted autolink tests `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.autolink.* --tests com.brainx.intelligence.infrastructure.dev.autolink.NoteAutoLinkApplicationRunnerTest --console=plain` 통과. 전체 `.\gradlew.bat --no-daemon check --console=plain`, `git diff --check` 통과. 실제 `COMPARE` relation 평가도 exit code 0으로 완료했다.

2026-06-25 17:41 - RAG chunk partial reindex

완료: RAG note chunk index가 매번 전체 교체되지 않도록 stable `chunkId`와 chunk manifest 기반 delta indexing을 추가했다. `MarkdownNoteChunker`는 `noteId::sha256(chunkText)::duplicateOrdinal` chunk id를 만들고, `WorkspaceNoteEventHandler`는 `intelligence_note_index_chunks` manifest와 새 snapshot chunk를 비교해 변경 chunk만 embedding/upsert한다. title 변경, manifest 없음, chunker version mismatch, recovery 상태, 변경률 50% 초과는 full replace로 유지한다. Qdrant adapter는 point id delete와 payload overwrite를 지원해 payload-only 변경에서는 Voyage를 호출하지 않는다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/events/note`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/note`, `src/main/java/com/brainx/intelligence/infrastructure/vector/qdrant`, `src/main/java/com/brainx/intelligence/exploration/application/port/outbound/NoteSearchIndexPort.java`, `docs/technical/note-chunking.md`, `docs/technical/vectorstore-embedding-model.md`

검증: targeted tests `.\gradlew.bat --no-daemon test --tests "*MarkdownNoteChunkerTest" --tests "*NoteChunkIndexPlannerTest" --tests "*WorkspaceNoteEventHandlerTest" --tests "*QdrantNoteSearchIndexAdapterTest" --tests "*NoteChunkManifestJpaAdapterTest" --console=plain` 통과. 전체 `.\gradlew.bat --no-daemon clean check --console=plain` 통과. `git diff --check` 통과. 문서 라우팅은 `rg "note-chunking|vectorstore-embedding-model|intelligence_note_index_chunks" docs vaults README.md`로 확인했다. 운영 DB는 `ddl-auto=validate`이므로 `intelligence_note_index_chunks` table 생성 DDL을 별도로 적용해야 한다.

## 2026-06-26 11:09 - 노트 링크 추천 Public API 구현

완료: OpenAPI 계약 변경 없이 `POST /api/v1/ai/link-suggestions`를 구현했다. `connection` 도메인/application/web 계층을 추가하고, 요청 `noteId`는 v1 정책대로 `default` document group의 색인 완료 note projection에서 확인한다. 권한은 `LINK_SUGGESTIONS` capability로 확인하고, 기존 `NoteAutoLinkUseCase`의 `VECTOR_LLM` 전략을 호출한 뒤 요청 source note의 suggestion만 `suggestionId`, `targetNoteId`, `targetTitle`, `score`, `reason`으로 반환한다. 반환된 suggestion마다 connection event port를 통해 `AiSuggestionCreated` payload를 발행한다. source note 없음, 권한 거절, note limit 초과, AI provider 미가용은 각각 404/403/409/500 wrapper로 매핑했다.

아티팩트: `src/main/java/com/brainx/intelligence/connection`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/note/NoteProjectionJpaAdapter.java`, `src/main/java/com/brainx/intelligence/infrastructure/events`, `src/main/java/com/brainx/intelligence/infrastructure/web/GlobalApiExceptionHandler.java`, `vaults/agents/domain-implementation-order.md`

검증: targeted tests `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.connection.application.usecase.ConnectionServiceTest --tests com.brainx.intelligence.connection.adapter.web.ConnectionControllerTest --tests com.brainx.intelligence.infrastructure.persistence.jpa.note.NoteProjectionJpaAdapterTest --tests com.brainx.intelligence.infrastructure.events.producer.KafkaIntelligenceEventAdapterTest --console=plain` 통과. 전체 `.\gradlew.bat --no-daemon check --console=plain`, `.\gradlew.bat --no-daemon compileDevUiJava --console=plain`, `git diff --check` 통과.

## 2026-06-26 12:04 - 브릿지 문서 후보 생성 API 구현

완료: `POST /api/v1/ai/bridge-concepts`를 입력 note 목록 사이를 이어 줄 새 문서/주제 후보 생성 API로 구현했다. OpenAPI SSOT의 `BridgeConceptsRequest`를 `noteIds[]` 계약으로 변경하고 local OpenAPI slice와 `brainx-next` generated intelligence 타입을 갱신했다. `connection` application/web 계층에 `CreateBridgeConceptsUseCase`를 추가하고, default document group의 active note title/tags만 prompt에 넣어 `AiChatPort.generate(...)`로 strict JSON 후보를 생성한다. 응답 `noteId`에는 `bridge-<sha256>` proposal id를 담고, 권한은 `LINK_SUGGESTIONS`, featureId는 `bridge-concepts`를 사용한다. provider usage가 있으면 실제 usage, 없으면 문자 길이 기반 추정 usage를 `TokenUsageRecordedRequested`로 기록하며 recommendation마다 `AiSuggestionCreated`를 발행한다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `../../brainx-next/lib/generated/intelligence-openapi.ts`, `src/main/java/com/brainx/intelligence/connection`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/note/NoteProjectionJpaAdapter.java`, `src/main/java/com/brainx/intelligence/infrastructure/events`, `src/main/resources/application.yaml`, `vaults/agents/domain-implementation-order.md`

검증: bundled Python으로 `scripts\extract_intelligence_openapi.py` 통과, `npm run generate:intelligence-api-types` 통과, `npm run typecheck` 통과. targeted tests `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.connection.application.usecase.ConnectionServiceTest --tests com.brainx.intelligence.connection.adapter.web.ConnectionControllerTest --tests com.brainx.intelligence.infrastructure.persistence.jpa.note.NoteProjectionJpaAdapterTest --tests com.brainx.intelligence.infrastructure.events.producer.KafkaIntelligenceEventAdapterTest --console=plain` 통과. 전체 `.\gradlew.bat --no-daemon check --console=plain`, `.\gradlew.bat --no-daemon compileDevUiJava --console=plain`, `git diff --check` 통과.

## 2026-06-26 14:20 - Connection API 문서 라우팅 추가

완료: `agent-documentation-compounder` 기준으로 pull 이후 확인한 public connection API 변경사항을 root guide에 넣지 않고 lazy-loaded 기술 문서로 분리했다. `docs/technical/connection-api.md`를 추가해 `POST /api/v1/ai/link-suggestions`, `POST /api/v1/ai/bridge-concepts`, 내부 `NoteAutoLinkUseCase` 재사용 방식, 현재 `default` document group 제한, source note eligibility, entitlement/usage/event/error mapping을 정리했다. 기존 `docs/technical/note-auto-linking.md`에는 public `connection` API와 내부 `autolink` 기능의 경계를 보강했다.

아티팩트: `docs/technical/connection-api.md`, `docs/technical/README.md`, `docs/technical/note-auto-linking.md`, `vaults/INDEX.md`

검증: 문서 전용 변경이라 Gradle test는 생략했다. 라우팅/경로 검색 `rg -n "connection-api|link-suggestions|bridge-concepts|default document group|NoteAutoLinkUseCase" docs/technical vaults/INDEX.md`, `rg -n "docs/technical/connection-api.md|connection-api.md" vaults/INDEX.md docs/technical/README.md docs/technical/note-auto-linking.md` 통과.

## 2026-06-26 15:06 - LLM 품질 CLI/평가 보강

완료: public REST/OpenAPI 계약 변경 없이 dev-only LLM 품질 capture를 보강했다. RAG capture script는 JSON scenario의 expected context/title/filename, min score, usage feature, answer text 조건을 검증하고 실패 시 exit code `1`을 반환한다. Chat router capture는 router model 하드코딩을 제거하고 route별 citation/event/answer acceptance rule을 추가했다. External search, inline assist, connection 품질용 capture script를 새로 추가했고, inline assist와 connection은 application use case를 직접 호출하는 dev runner를 추가했다.

아티팩트: `scripts/capture_sample_rag_outputs.py`, `scripts/capture_chat_router_cli.py`, `scripts/capture_external_search_cli.py`, `scripts/capture_inline_assist_cli.py`, `scripts/capture_connection_cli.py`, `src/main/java/com/brainx/intelligence/infrastructure/dev/assist`, `src/main/java/com/brainx/intelligence/infrastructure/dev/connection`, `src/test/java/com/brainx/intelligence/infrastructure/dev/assist`, `src/test/java/com/brainx/intelligence/infrastructure/dev/connection`, `src/main/resources/application.yaml`, `docs/technical/sample-notes-rag-cli.md`, `docs/technical/external-search.md`, `docs/technical/frontend-ai-context-management.md`, `docs/technical/connection-api.md`

검증: `python -m py_compile scripts\capture_sample_rag_outputs.py scripts\capture_sample_rag_retrieval.py scripts\capture_sample_rag_chat.py scripts\capture_chat_router_cli.py scripts\capture_external_search_cli.py scripts\capture_inline_assist_cli.py scripts\capture_connection_cli.py` 통과. Targeted dev tests `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.dev.* --console=plain` 통과. 전체 `.\gradlew.bat --no-daemon check --console=plain`, `git diff --check` 통과.

실제 평가: 최초 `python scripts\capture_sample_rag_retrieval.py --run-name 20260626-rag-retrieval-quality`는 Qdrant gRPC `localhost:6334` 연결 거부로 ingest 실패했다. Docker Desktop을 시작하고 `docker compose up -d qdrant` 후 재실행한 `build/rag-captures/20260626-rag-retrieval-quality-qdrant`는 12개 중 실패 0, context count `[5,8,7,8,8,8,4,7,8,8,8,0]`였다. `build/rag-captures/20260626-rag-chat-quality`도 12개 실패 0이었다. `build/chat-router-captures/20260626-chat-router-quality`는 5개 route가 `NOTE_QA`, `WORKSPACE_SEARCH`, `COMPOSE`, `NOTE_ACTION`, `OUT_OF_SCOPE`로 모두 통과했다. 최초 external search run은 20초 provider timeout으로 실패했고, script 기본 timeout을 60초로 올린 뒤 `build/external-search-captures/20260626-external-search-quality-timeout60`에서 2개 실패 0, source count `[8,8]`로 통과했다. `build/inline-assist-captures/20260626-inline-assist-quality`는 4개 실패 0, `build/connection-captures/20260626-connection-quality`는 link suggestion 1개와 bridge recommendation 3개로 실패 0이었다.

## 2026-06-26 15:28 - LLM 품질 평가 보고서 작성

완료: 실제 provider 호출로 생성한 RAG retrieval/chat, chat router, external search, inline assist, connection capture 결과를 `docs/technical/llm-quality-evaluation-report-2026-06-26.md`에 보고서 형태로 정리했다. 최종 pass 결과와 함께 Qdrant 미기동 실패, external search 20초 timeout 실패, 60초 timeout 재평가 조치를 분리해 남겼다. `docs/technical/README.md`와 `vaults/INDEX.md`에 라우팅도 추가했다.

검증: 문서 전용 변경이므로 Gradle 검증은 생략했다. `rg -n "llm-quality-evaluation-report|LLM 품질 CLI 평가 보고서|20260626-rag-retrieval-quality-qdrant|20260626-external-search-quality-timeout60|Connection" docs/technical vaults/INDEX.md vaults/worklogs/2026-06.md`와 `git diff --check`로 문서 링크/공백을 확인했다.

## 2026-06-26 15:39 - LLM 품질 영역별 상세 보고서 작성

완료: 요약 보고서에서 부족했던 의미적 해석을 보강하기 위해 `docs/technical/llm-quality-evaluations/` 아래에 RAG, chat router, external search, inline assist, connection 상세 보고서 5개를 추가했다. 각 문서에는 실제 capture 결과의 질의/문서 연결/source/citation/output과 해당 기능의 prompt/request 구조를 함께 정리했다. 특히 connection 문서에는 `BrainX API 명세서.md`에서 `BrainX 도메인 기준 MSA / API / 이벤트 계약`으로 추천된 link suggestion과 bridge 후보 3개, autolink/bridge prompt 구조, public DTO에서 anchor가 빠져 있어 후속 diagnostic capture가 필요하다는 점을 남겼다.

검증: 문서 전용 변경이므로 Gradle 검증은 생략했다. `rg -n "llm-quality-evaluations|RAG 품질 상세 평가|Chat Router 품질 상세 평가|External Search 품질 상세 평가|Inline Assist 품질 상세 평가|Connection 품질 상세 평가|BrainX API 명세서.md|BrainX 도메인 기준 MSA" docs/technical vaults/INDEX.md vaults/worklogs/2026-06.md`와 `git diff --check`로 문서 링크/공백을 확인했다.

## 2026-06-29 11:40 - AI 폴더 정리 제안 API 구현

완료: OpenAPI 계약 변경 없이 `POST /api/v1/ai/folder-organization-proposals`를 구현했다. `organization` domain/application/web 계층에 폴더 정리 제안 use case를 추가하고, default document group의 active/searchable note card(`folderId`, `title`, `tags`, `headings`, `excerpt`)를 기반으로 `AiChatPort.generate(...)`를 호출해 `proposedFolders`, `proposedMoves` open object 배열을 반환하도록 했다. 권한 capability는 `FOLDER_ORGANIZATION`, featureId는 `folder-organization`으로 고정했고, 성공 시 proposal 단위 `AiSuggestionCreated`와 provider/추정 token usage를 기록한다. `scope=all` note 없음은 409, `scope=folder` note 없음은 404, provider 실패는 500 wrapper로 매핑했다.

아티팩트: `src/main/java/com/brainx/intelligence/organization`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/note/NoteProjectionJpaAdapter.java`, `src/main/java/com/brainx/intelligence/infrastructure/events`, `src/main/java/com/brainx/intelligence/infrastructure/web/GlobalApiExceptionHandler.java`, `src/main/resources/application.yaml`, `vaults/agents/domain-implementation-order.md`

검증: targeted tests `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.organization.*" --tests "com.brainx.intelligence.infrastructure.persistence.jpa.note.NoteProjectionJpaAdapterTest" --tests "com.brainx.intelligence.infrastructure.events.producer.KafkaIntelligenceEventAdapterTest" --console=plain` 통과. 전체 `.\gradlew.bat --no-daemon check --console=plain`, `.\gradlew.bat --no-daemon compileDevUiJava --console=plain`, `git diff --check` 통과.
