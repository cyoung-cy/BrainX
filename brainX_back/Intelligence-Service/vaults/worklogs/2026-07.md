# 2026-07 Worklog

## 2026-07-01 03:36 - Graph AI 연결 추천 UI

완료: `brainx-next` `/graph`에 기존 징검다리 추천과 별도의 `AI 연결 추천` 선택 모드를 추가했다. 노드 클릭 또는 `Shift + 드래그`로 1~10개 source note를 선택하고, source별 `POST /api/v1/ai/link-suggestions` 호출 결과를 source 그룹으로 보여준다. 후보별 `수락`과 `전체 수락`은 Workspace `POST /api/v1/notes/{noteId}/links`를 호출해 실제 링크를 만들고, 성공 즉시 그래프 edge와 note links를 optimistic update한다. 공개 OpenAPI/AsyncAPI와 백엔드 로직은 변경하지 않았다.

아티팩트: `../../brainx-next/components/graph-screen.tsx`, `../../brainx-next/lib/intelligence-api.ts`, `../../brainx-next/lib/workspace-api.ts`, `../../README.md`

검증: `npm run typecheck` 통과.

## 2026-07-01 05:44 - Chat 초안 제목 및 개인 노트 톤

완료: `/chat` 초안 계열 라우트(`COMPOSE`, `NOTE_ACTION`)의 LLM system prompt에 첫 줄 `# <title>` Markdown heading, 한 줄 공백 뒤 본문, 개인 노트 작성 톤 규칙을 추가했다. 일반 RAG Q&A에는 제목 강제가 섞이지 않도록 테스트를 보강했고, 프론트 저장 fallback은 heading/thread title이 없을 때 `AI 초안`으로 떨어지도록 조정했다. OpenAPI SSOT와 Intelligence 로컬 OpenAPI 슬라이스에는 초안 SSE delta가 제목 heading으로 시작하는 Markdown 문자열임을 반영했다. AsyncAPI 구조 변경은 없다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatService.java`, `src/test/java/com/brainx/intelligence/chat/application/usecase/ChatServiceTest.java`, `../../brainx-next/components/chat-screen.tsx`, `../../README.md`

검증: 번들 Python으로 `scripts\extract_intelligence_openapi.py` 통과. `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.chat.application.usecase.ChatServiceTest --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). 문서 라우팅 검색 `rg "vaults/|AGENTS.md|knowledge-intelligence.openapi.yaml|knowledge-intelligence.asyncapi.yaml|knowledge-intelligence.consumed.openapi.yaml" README.md AGENTS.md vaults src/main/resources/contracts/README.md` 통과.

## 2026-07-01 12:34 - Bridge wiki-link 보장

완료: `bridge-concepts` 후보의 `bridgeReason`이 입력 순서의 첫 두 source note title을 `[[노트 제목]]` 형식으로 포함하도록 prompt와 후처리를 보강했다. 3개 이상 note를 선택해도 실제 bridge note가 직접 wiki-link로 연결하는 원본 concept은 앞의 두 note로 제한하고, 나머지는 후보 생성 배경으로만 쓰도록 명시했다. `/graph`에서 징검다리 후보 저장 시 생성되는 note markdown도 `/notes/{id}` 링크 대신 앞의 두 source note를 `[[...]]` wiki link로 저장하도록 맞췄다.

아티팩트: `src/main/java/com/brainx/intelligence/connection/application/usecase/ConnectionService.java`, `src/main/java/com/brainx/intelligence/infrastructure/dev/connection/ConnectionQualityApplicationRunner.java`, `scripts/capture_connection_cli.py`, `../../brainx-next/components/graph-screen.tsx`, `docs/technical/connection-api.md`, `../../README.md`

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.connection.application.usecase.ConnectionServiceTest --tests com.brainx.intelligence.infrastructure.dev.connection.ConnectionQualityApplicationRunnerTest --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. `npm run typecheck` 통과. 실제 provider 기반 `capture_connection_cli.py`는 `QDRANT_COLLECTION=brainx_note_search_voyage_1024`, `SERVER_PORT=0`으로 `20260701-bridge-wikilink-quality-port0` run을 실행해 link/bridge scenario 모두 통과했고, bridge recommendation 3개가 두 원본 wiki link를 포함했다. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-01 12:34 - Graph AI selection drag hit-test

완료: `/graph`의 `징검다리 개념 추천`과 `AI 연결 추천` 선택 모드에서 `Shift + 드래그` 범위가 React Flow DOM 박스/라벨 폭 기준으로 과하게 잡히던 문제를 보정했다. 드래그 중 선택 후보를 React Flow 기본 `Partial` selection 결과가 아니라 현재 selection rectangle을 flow 좌표로 변환한 뒤 노드 중심점이 rectangle 안에 있는지로 직접 계산한다. 드래그 시작 전 선택은 baseline으로 유지하고, 드래그 중 잠깐 잡힌 노드를 누적하지 않도록 현재 rectangle 결과로 replace한다.

아티팩트: `../../brainx-next/components/graph-screen.tsx`

검증: `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). `localhost:3002/graph`에서 Playwright 일회성 확인으로 `징검다리 개념 추천`, `AI 연결 추천` 모두 같은 `Shift + 드래그` 범위가 `3`개 선택으로 들어가는 것을 확인.
